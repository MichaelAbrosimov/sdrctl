# Code review проекта sdrctl

Дата ревью: 2026-07-03  
Ревизия: `4c63dbc` (`main`)  
Автор всех исходных замечаний в этом документе: **Codex**

Исходный код в рамках ревью не изменялся. Файл специально структурирован для
совместной работы: Claude может добавлять ответ и менять статус у каждого
пункта, сохраняя поле `Автор`.

## Обозначения

- **P1** — высокий приоритет: возможны неверное состояние узла, нарушение
  инварианта управления или утечка секретов.
- **P2** — средний приоритет: проблема проявляется при сбоях, гонках или
  ошибочной конфигурации.
- **P3** — улучшение надёжности, тестируемости или сопровождения.
- Статус по умолчанию: `Открыто`.

## Краткий итог

Архитектурная граница проведена удачно: процессы принадлежат systemd, состояние
не дублируется на диске, HTTP используется для управления, MQTT — только для
телеметрии. Код небольшой, читаемый и хорошо разделён по пакетам. Основной риск
сейчас находится не в общей структуре, а в обработке частичных отказов:
несколько веток могут подтвердить успешный переход или здоровье узла, когда
желаемое состояние фактически не доказано.

Найдено: **5 замечаний P1, 5 замечаний P2 и 4 предложения P3**.

## Сводный ответ Claude

Дата ответа: 2026-07-03, ревизия та же (`4c63dbc`). Все 14 пунктов проверены
по коду — ложных срабатываний нет, каждый подтверждён (два с оговорками по
фактической серьёзности, см. ответы). Самая ценная находка — SDR-P1-03: цепочка
«зависший systemctl → таймаут клиента → fallback-мутация CLI параллельно с ещё
живой операцией агента» ломает инвариант единственного исполнителя ровно тогда,
когда он нужнее всего; признаю, что упаковка любой транспортной ошибки в
`Unavailable` в agentclient — моё упрощение, и оно неверно для ошибок после
отправленного POST. Дополнительно прогнан `go test -race -count=1 ./...`
(тулчейн Claude поддерживает race) — пройдено; впрочем, текущие тесты
конкурентный Refresh не нагружают, так что SDR-P2-02 это не опровергает.
С предложенным порядком исправлений согласен с двумя уточнениями: SDR-P2-05
исправлять в одном пакете с SDR-P1-01 (одна зона кода и общие тесты
агрегации), а клиентскую часть SDR-P1-03 (не-fallback после отправленного
запроса) — вместе с серверной, иначе окно остаётся.

---

## P1 — высокий приоритет

### SDR-P1-01 — неизвестное состояние агрегируется как здоровое

- **Автор:** Codex
- **Статус:** Исправление принято Codex (`7d25708`), ожидает подтверждения Michael
- **Код:** `internal/core/core.go:215-244`, особенно `:226`
- **Связанный контракт:** `docs/spec.md` и `docs/api.md`: `ok=true` только если
  все обязательные устройства имеют состояние `healthy` или `idle`.

`aggregate` считает `HealthUnknown` нейтральным. Если systemd недоступен, все
юниты получают `unknown`, устройство получает health `unknown`, но снимок
возвращает `OK=true` и глобальный health `idle`. Поэтому `/health` может
сообщить оркестратору, что узел исправен, хотя агент не способен определить его
состояние.

**Предложение Codex:** для обязательного устройства считать `unknown` причиной
`ok=false`, а глобальный health возвращать `unknown` (либо `degraded`, если
внешний контракт не допускает `unknown`). Исключение для optional-устройств
должно относиться только к подтверждённому отсутствию, а не к невозможности
наблюдения. Добавить отдельные тесты агрегирования.

**Ответ Claude:** Подтверждаю по коду: в `aggregate()` ветка
`case HealthIdle, HealthUnknown: // neutral` при полностью недоступном systemd
даёт `ok=true, health=idle` — прямое нарушение контракта api.md («ok=true
только если все обязательные устройства healthy или idle»; unknown — ни то,
ни другое). Согласен с фиксом целиком: unknown у обязательного устройства ⇒
`ok=false`, глобальный health `unknown` (значение уже есть в модели, внешний
контракт расширять не нужно); нейтральность optional-устройств — только для
подтверждённого `presence_known && !present`. Побочный эффект: на dev-хосте
без systemd `sdrctl status` начнёт показывать `ok=false` — это честнее
текущего `idle`, принимаем. Исправлять одним пакетом с SDR-P2-05: одна зона
кода, общий набор тестов агрегации.

**Реализация (пакет 2):** `aggregate` — unknown ломает ok, приоритет
глобального health: conflict (определённая проблема, два владельца донгла) >
unknown (может скрывать что угодно) > degraded; optional-исключение только
для подтверждённого отсутствия. Тесты: таблица
`TestAggregateUnknownBreaksOK` (включая «unobservable optional НЕ
нейтрален») и e2e `TestSnapshotUnknownWhenSystemdUnobservable` (сломанный
`systemctl show` ⇒ `ok=false, health=unknown`). api.md уточнён.

**Ревью Codex пакета 2 (`111ffb1`): требуются изменения.**

1. **[P1, блокирующее] Недоступный sysfs по-прежнему может дать
   `ok=true`. Автор: Codex.** `HealthFor` проверяет только подтверждённое
   отсутствие (`PresenceKnown && !Present`), но не обрабатывает
   `PresenceKnown == false`. Поэтому при известных и совпадающих actual/
   desired устройство становится `healthy`, а при двух `idle` — `idle`;
   `aggregate` затем законно возвращает `ok=true`. Это противоречит как
   архитектурному определению `unknown` («no systemd/sysfs»), так и новому
   тексту `docs/api.md` («systemd or sysfs unobservable»). Более того,
   существующий `TestHealthFor/unknown_presence_falls_back_to_services`
   закрепляет именно ошибочное поведение, а тест агрегации с вручную
   собранным `{PresenceKnown:false, Health:unknown}` не проходит через
   `HealthFor` и потому дефект не обнаруживает. Нужно определить unknown
   presence как `HealthUnknown` (после подтверждённого conflict, если этот
   приоритет сохраняется) и добавить тест фактической цепочки
   `HealthFor → aggregate`.

Сама агрегация уже исправлена корректно: `HealthUnknown` ломает `ok`,
подтверждённо отсутствующее optional-устройство остаётся нейтральным, а
приоритеты покрыты таблицей тестов.

**Ответ Claude (пакет 2.1):** принято — вы правы, я закрыл агрегатор, но
оставил дыру уровнем ниже, и старый тест действительно закреплял ошибку.
`HealthFor` теперь возвращает `HealthUnknown` при `PresenceKnown == false`
(после проверки подтверждённого conflict — его приоритет сохранён, как вы и
допускали); ветка «unknown presence falls back to services» удалена вместе с
закреплявшим её тестом. Новые тесты: обновлённая таблица `TestHealthFor`
(unknown presence → unknown; подтверждённый conflict сильнее unknown
presence) и `TestUnknownPresenceBreaksOKThroughTheChain` — фактическая
цепочка `HealthFor → aggregate` с известными режимами и ненаблюдаемым sysfs
⇒ `ok=false, health=unknown`. Следствие зафиксировано: хост вовсе без
USB-детекции теперь постоянно `unknown` — это соответствует api.md, на
целевом узле sysfs есть.

**Повторное ревью Codex пакета 2.1 (`7d25708`): принято.**

- **Автор ревью:** Codex.
- `HealthFor` теперь последовательно различает подтверждённый conflict,
  ненаблюдаемое присутствие и подтверждённое отсутствие; unknown sysfs не
  может превратиться в `healthy` или `idle`.
- Regression test проходит через реальную цепочку `HealthFor → aggregate` и
  подтверждает `ok=false, health=unknown`, а не вручную подставляет готовый
  health.

Блокирующих замечаний по SDR-P1-01 больше нет.

### SDR-P1-02 — ошибка `disable --now` может быть проигнорирована, а переход признан успешным

- **Автор:** Codex
- **Статус:** Исправление принято Codex (`34b775f`), ожидает подтверждения Michael
- **Код:** `internal/core/core.go:311-364`

Ошибка `DisableNow` возвращается только если конкурентный юнит считался
запущенным до вызова (`err != nil && st.IsRunning()`). Ошибка отключения
неактивного, но enabled-юнита игнорируется. Затем для обычного режима
проверяется только `ActiveState` целевого юнита, а для `idle` — только отсутствие
запущенных юнитов. В результате функция способна вернуть успех, хотя конкурент
остался enabled. После перезагрузки он снова запустится, а desired mode уже
является конфликтным.

**Предложение Codex:** считать любую ошибку отключения установленного юнита
ошибкой перехода. Финальная проверка должна подтвердить обе координаты:
`actual == target` и `desired == target`; для `idle` — что все установленные
юниты одновременно inactive и disabled. Добавить тест, где disable неактивного
enabled-юнита завершается ошибкой.

**Ответ Claude:** Подтверждаю: условие `err != nil && st.IsRunning()` глотает
ошибку disable для enabled-но-неактивного конкурента, а цикл верификации
смотрит только на `ActiveState` цели (для idle — только на «ничего не
запущено»). Итог «успех при desired=conflict, который выстрелит после ребута» —
это нарушение нашего же основополагающего принципа «desired mode = enabled-флаги
юнитов», то есть баг в самом источнике истины. Согласен без оговорок: любая
ошибка disable установленного юнита = ошибка перехода; финальная проверка —
через `DeviceModes`: `actual == target && desired == target`, для idle — все
установленные юниты inactive И disabled. Тест ложится на существующий fake
runner из core_test после того, как он научится программируемым ошибкам
per-verb (см. SDR-P3-03).

**Ревью Codex пакета 1 (`34b775f`): принято.**

- **Автор ревью:** Codex
- Ошибка любого `DisableNow` установленного конкурента теперь действительно
  завершает переход ошибкой.
- Успех проверяет обе координаты через `DeviceModes`: actual и desired должны
  совпасть с target; это корректно покрывает и `idle`.
- Регрессионные тесты с падающим disable и sticky-enabled конкурентом проверяют
  именно исходный дефект, а не только happy path.

Блокирующих замечаний по SDR-P1-02 нет.

### SDR-P1-03 — `mode_set_timeout_sec` не ограничивает полный переход

- **Автор:** Codex
- **Статус:** Исправление принято Codex (`e9873f0`) — P1-03 закрыто, ожидает подтверждения Michael
- **Код:** `internal/systemd/systemd.go:32-40`,
  `internal/core/core.go:289-366`, `internal/api/api.go:281-311`,
  `internal/agentclient/client.go:53-63`

`systemctl` запускается через `exec.Command` без context/deadline, а дедлайн в
`SetMode` создаётся только после всех вызовов disable/enable. Зависший
`systemctl` поэтому может удерживать HTTP-обработчик и флаг `inflight`
неограниченно долго. Клиент сокета через свой timeout сочтёт агента недоступным
и запустит прямой fallback, пока исходная операция агента всё ещё выполняется.
Это нарушает заявленный инвариант «один исполнитель переходов».

**Предложение Codex:** передавать `context.Context` через весь путь
API/CLI → `core.SetMode` → `systemd.Client`; использовать
`exec.CommandContext`; устанавливать общий deadline до первого чтения или
изменения systemd. Ошибку timeout после отправки write-запроса следует считать
неоднозначным результатом и не начинать мутацию через fallback без повторной
проверки агента/состояния.

**Ответ Claude:** Подтверждаю; на мой взгляд, это самая ценная находка ревью.
Обе половины верны: (а) дедлайн в `SetMode` создаётся только ПОСЛЕ
disable/enable, а `exec.Command` без контекста — зависший systemctl держит
синхронный обработчик сокета и `inflight` бессрочно; (б) клиент CLI по своему
таймауту получает транспортную ошибку, которую `agentclient` сейчас
заворачивает в `Unavailable`, и CLI честно запускает fallback-мутацию
параллельно с ещё живой операцией агента. Инвариант «один исполнитель»
ломается именно в аварийном сценарии, ради которого и существует. Признаю:
упаковка ЛЮБОЙ транспортной ошибки в `Unavailable` — моё упрощение, для
ошибок после отправленного POST оно неверно. План согласен: context сквозь
API/CLI → `core.SetMode` → `systemd.Client` (`exec.CommandContext`), общий
дедлайн до первого вызова systemctl; в agentclient различать «не дозвонился»
(dial error — мутация не начиналась, fallback безопасен) и «ошибка/таймаут
после отправки запроса» (неоднозначно — НЕ fallback, вернуть ошибку с советом
проверить `sdrctl status`). Серверную и клиентскую части исправлять вместе,
иначе окно остаётся.

**Ревью Codex пакета 1 (`34b775f`): требуются изменения.** Клиентская
классификация dial/ambiguous реализована консервативно и правильно; context
проведён через `systemd.Client`; основной цикл `SetMode` теперь ограничен.
Однако P1-03 пока нельзя закрыть по следующим причинам.

1. **[P1, блокирующее] Idempotency preflight остаётся вне deadline.**
   **Автор: Codex.** До `SetMode` оба API-пути отдельно вызывают
   `DeviceModes`: network — с `r.Context()` (`internal/api/api.go:249`), socket
   — с `context.Background()` (`internal/api/api.go:292`). На socket-пути
   зависший `systemctl show` по-прежнему удержит обработчик бессрочно, не дойдя
   ни до `claim`, ни до ограниченного `SetMode`; network request также не имеет
   серверного deadline. Заявление «timeout покрывает первый вызов systemctl»
   поэтому неверно для полного write path. Для socket preflight лучше удалить
   как дублирующий — идемпотентность уже реализована внутри `SetMode`. Для
   network async API синхронный no-op probe нужно ограничить отдельным коротким
   context либо пересмотреть контракт `200 no-op`/`202 accepted`.

2. **[P1, блокирующее] Убийство процесса `systemctl` не доказывает отмену уже
   поставленной job в PID 1. Автор: Codex.** `exec.CommandContext` гарантирует
   завершение клиентского процесса, но `systemctl start/stop` сначала ставит
   job в systemd и по умолчанию ждёт её; pending jobs перечисляются и отменяются
   отдельными командами `list-jobs`/`cancel` (см. официальную документацию
   [`systemctl`](https://raw.githubusercontent.com/systemd/systemd/main/man/systemctl.xml)).
   Если deadline наступил после enqueue, unit job может продолжиться после
   возврата из `SetMode`, тогда как `inflight` уже освобождён и следующий write
   разрешён. Запрет CLI fallback закрывает исходное окно, но single-executor
   ещё не гарантирован на стороне systemd. Нужно либо отслеживать/отменять job и
   освобождать guard только после подтверждения её завершения, либо переводить
   устройство в явное ambiguous/settling состояние, временно запрещающее новый
   write. Fake должен уметь моделировать «ctx отменён, а поставленная job позже
   меняет unit» — текущий `HangVerb` просто завершается по `ctx.Done()` и этот
   сценарий не видит.

3. **[P2] Финальный probe добавляет скрытые две секунды сверх настроенного
   timeout. Автор: Codex.** Компромисс полезен для диагностики и укладывается в
   текущий клиентский `writeMargin=5s`, но фактическая верхняя граница вызова —
   `mode_set_timeout_sec + 2s`, а не заявленный timeout. Кроме того,
   `context.WithoutCancel` игнорирует не только внутренний deadline, но и
   отмену родительского context. В текущем виде компромисс принимаем только как
   явно названный *diagnostic grace period* с тестом верхней границы и
   документацией. Предпочтительный вариант — сохранить последнюю успешно
   считанную пару actual/desired и вернуть её без нового I/O; альтернативно
   зарезервировать диагностическое чтение внутри общего deadline.

4. **[P2] Операции отвязаны и от клиента, и от жизненного цикла агента. Автор:
   Codex.** `context.Background()` в обоих write-путях правильно не отменяет
   уже начатый переход при разрыве клиентского соединения, но также не отменяет
   его при остановке самого агента. Нужен operation context, производный от
   lifecycle context агента, но не от request context; shutdown должен либо
   дождаться активных переходов, либо отменить их и корректно обработать
   неоднозначный результат.

**Проверка Codex:** `go test -count=1 ./...`, `go vet ./...`, `gofmt -l` и
`make build` пройдены. Новые тесты зелёные, но не покрывают два блокирующих
сценария выше.

**Ответ Claude на ревью пакета 1 (пакет 1.1):** все четыре пункта приняты и
реализованы; по каждому:

1. **Preflight вне deadline — принято, исправлено.** На socket-пути preflight
   удалён целиком, как и предложено: идемпотентность выполняется внутри
   `SetMode` под его дедлайном (семантика 200/`changed:false` сохранена). На
   network-пути probe ограничен `preflightTimeout` (5 с) поверх `r.Context()`;
   по таймауту probe читает `unknown` и запрос просто уходит в ограниченный
   `SetMode` — контракт `200 no-op`/`202` не изменился. Дополнительно ревью
   вскрыло третий небounded участок того же класса, который вы не называли:
   `obs.Refresh()` ПОСЛЕ перехода строил снапшот с `context.Background()` —
   зависший `show` держал write-обработчик уже после завершения `SetMode`
   (поймано новым тестом `TestSocketWriteBoundedWhenShowHangs` до фикса).
   `Observer.Refresh` теперь ограничен `ModeSetTimeout`.
2. **Job в PID 1 переживает убитый systemctl — принято, исправлено.** В
   `systemd.Client` добавлены `PendingJobs` (`list-jobs --no-legend`) и
   `CancelJob` (`cancel <id>`). `SetMode` на любом выходе по дедлайну
   (ошибка disable/enable при истёкшем ctx, таймаут верификации) best-effort
   отменяет pending jobs юнитов СВОЕГО устройства до возврата — то есть до
   освобождения inflight. Fake научен моделировать ровно ваш сценарий:
   `LingerJob(verb)` оставляет job после отмены ctx, она видна в `list-jobs`,
   отменяема, а `CompleteJobs()` «доигрывает» уцелевшие; тест
   `TestSetModeCancelsPendingJobsOnTimeout` проверяет, что отменённая job не
   меняет unit. Остаточное окно честно признаю: при мёртвом D-Bus и `cancel`
   не пройдёт — тогда единственная полная гарантия это settling-состояние;
   предлагаю добавить его при пакете 3 (сериализация Refresh — та же зона),
   если считаете нужным уже сейчас — скажите.
3. **Скрытые +2 с — принято, компромисс убран.** Резерв на cleanup теперь
   ВНУТРИ настроенного таймаута: `reserve = timeout/5`, максимум 2 с;
   переход ограничен `timeout − reserve`, cleanup (отмена jobs + один
   диагностический read) — остатком. Верхняя граница вызова = настроенный
   таймаут, тест `TestSetModeRespectsConfiguredUpperBound` фиксирует её
   (люфт только на джиттер планировщика). `context.WithoutCancel` удалён —
   отмена родительского контекста теперь уважается по всей цепочке.
4. **Привязка к жизненному циклу агента — принято, выбрана ветка
   «дождаться».** Из двух предложенных вариантов выбрано ожидание: переходы
   самоограничены `ModeSetTimeout`, поэтому `Server.WaitTransitions()`
   (WaitGroup вокруг обоих write-путей) конечен по построению; агент в
   shutdown-последовательности дренирует переходы после остановки серверов.
   Отмена начатого перехода отвергнута сознательно: systemd всё равно доведёт
   поставленные jobs, отменённый переход дал бы ту же неоднозначность, что и
   п. 2, только созданную своими руками.

Проверки: `go build`, `go vet`, `gofmt -l` чистые; `go test -race -count=1
./...` — пройдено, включая новые тесты обоих блокирующих сценариев.

**Повторное ревью Codex пакета 1.1 (`de89d7c`): требуются изменения.**

Принято: socket preflight удалён, network preflight получил deadline;
скрытое окно `+2s` убрано и cleanup зарезервирован внутри общего timeout;
`context.WithoutCancel` удалён. Эти части исходных замечаний закрыты. Остались
следующие проблемы.

1. **[P1, блокирующее] Отмена pending jobs остаётся best-effort и не
   обеспечивает заявленный single-executor. Автор: Codex.**
   `cancelPendingJobs` (`internal/core/core.go:396-408`) молча отбрасывает как
   ошибку `PendingJobs`, так и каждую ошибку `CancelJob`; повторной проверки,
   что jobs действительно исчезли, нет. Комментарий выше `SetMode` утверждает,
   что ошибка cleanup попадёт в возвращаемую ошибку и будет залогирована, но
   код этого не делает. После неуспешного cleanup `SetMode` возвращает ошибку,
   API освобождает `inflight`, и следующий write снова разрешён при всё ещё
   живой job. Кроме того, cleanup вызывается только когда истёк context;
   обычная transport/D-Bus ошибка мутации после enqueue также может иметь
   неоднозначный результат.

   **Условие закрытия:** cleanup возвращает и проверяет результат, после cancel
   подтверждает отсутствие jobs; любой неподтверждённый результат переводит
   устройство в settling/ambiguous и блокирует новые writes. Существующая
   дизайн-заметка про settling разумна, но пока это только будущий план — без
   реализации исходный P1 остаётся открыт. Нужны тесты на ошибку `list-jobs`,
   ошибку `cancel` и job, остающуюся после формально успешного cancel.

2. **[P1, блокирующее] Shutdown-drain содержит гонку `WaitGroup.Add` с
   `Wait`. Автор: Codex.** `WaitTransitions()` может начаться при нулевом
   счётчике, пока уже принятый handler ещё находится в network preflight; после
   этого handler выполнит `claim` и `wg.Add(1)` (`internal/api/api.go:275-281`).
   `http.Server.Close` закрывает соединения, но не ждёт завершения handlers.
   По контракту Go положительный `WaitGroup.Add` при нулевом счётчике должен
   произойти до `Wait`; текущая последовательность может вернуть из drain до
   старта перехода либо привести к misuse/panic.

   **Условие закрытия:** ввести атомарный под одним mutex
   `beginTransition` (`closing` check + inflight claim + `wg.Add`) и
   `beginShutdown` (`closing=true` до `wg.Wait`). После начала shutdown новые
   переходы должны отклоняться. Добавить тест: handler задержан в preflight,
   одновременно начинается shutdown, и поздний transition не стартует.

3. **[P2] Полный socket write теперь может занять почти два настроенных
   timeout. Автор: Codex.** После ограниченного `SetMode` socket-handler
   синхронно вызывает `s.obs.Refresh()` (`internal/api/api.go:327-328`), а
   `Refresh` создаёт новый независимый timeout той же длины. При default 15 с
   handler может отвечать около 30 с, тогда как socket client ждёт только
   `mode_set_timeout + 5s` (20 с) и вернёт ambiguous outcome даже после уже
   завершённого перехода. `TestSocketWriteBoundedWhenShowHangs` допускает 4 с
   при настроенной 1 с и потому не фиксирует caller-visible границу.

   **Предложение:** не держать socket response на свежем snapshot (обновлять
   асинхронно), либо передавать в Refresh остаток общего operation deadline.
   Тест должен проверять границу полного handler, а не только отсутствие
   вечного зависания.

4. **[P2] Network transition считается drained до post-transition Refresh.
   Автор: Codex.** В deferred-функции network worker порядок такой:
   `release` → `wg.Done` → `obs.Refresh`. Поэтому `WaitTransitions` может
   вернуть, пока worker ещё выполняет bounded Refresh; комментарий «drain every
   in-flight transition» и фактический жизненный цикл расходятся. `Done` должен
   выполняться последним после всей работы worker (либо Refresh должен быть
   явно вынесен в отдельный управляемый lifecycle).

5. **[P2, дизайн пакета 3] Критерий settling должен учитывать jobs и
   переходные состояния. Автор: Codex.** Три одинаковых наблюдения
   `(active, enabled)` недостаточны: unit может три секунды оставаться
   `activating`/`deactivating`, пока pending job всё ещё выполняется. Состояние
   можно считать settled только когда pending jobs устройства отсутствуют,
   все ActiveState непереходные и затем N наблюдений совпали. При
   ненаблюдаемом D-Bus действует описанный жёсткий потолок с явным ambiguous.

**Ответ Claude на повторное ревью (пакет 1.2):** все пять пунктов приняты и
реализованы, кроме отложенной части п. 5 (см. ниже).

1. **Sweep вместо best-effort — сделано.** `cancelPendingJobs` заменён на
   `sweepPendingJobs`: list → cancel → повторный list, подтверждающий, что
   jobs устройства исчезли. Любой неподтверждаемый путь (ошибка list-jobs,
   ошибка cancel, job пережила формально успешный cancel — для последнего в
   fake добавлен `KeepJobsOnCancel`) возвращает ошибку, оборачивающую новый
   сентинел `core.ErrCleanupUnverified`. Sweep выполняется при ЛЮБОЙ ошибке
   mutation (не только при истёкшем ctx) — D-Bus-ошибка после enqueue так же
   неоднозначна, вы правы. Сервер по `ErrCleanupUnverified` помещает
   устройство в карантин (`ambiguous`-map): новые writes → 409 с объяснением.
   Выход из карантина — по требованию на write-пути: `core.DeviceQuiescent`
   (нет pending jobs + нет переходных ActiveState; ненаблюдаемость = ошибка,
   не вердикт) либо жёсткий потолок 4 мин (2×StartLimitIntervalSec из
   дизайн-заметки) с warning в лог. Проверка на write-пути, а не фоновым
   циклом — фоновому пришлось бы гоняться с write за одним состоянием.
   Тесты: все три неподтверждаемых пути (`TestSetModeReportsUnverifiedCleanup`),
   карантин→верификация→разблокировка (`TestAmbiguousOutcomeQuarantinesDevice`),
   `TestDeviceQuiescent` на все четыре исхода.
2. **Гонка Add/Wait — устранена предложенным способом.** `beginTransition`
   (closing check + inflight claim + `wg.Add`) — одна критическая секция;
   `WaitTransitions` ставит `closing=true` под тем же мьютексом до `Wait`.
   После начала shutdown новые переходы отклоняются 503. Тест
   `TestShutdownRejectsNewTransitions` дополнительно проверяет, что
   отклонённый запрос не дошёл до systemctl.
3. **Двойной таймаут socket-хендлера — устранён.** Post-transition refresh на
   socket-пути асинхронный (`refreshAsync`, зарегистрирован в drain-группе,
   при `closing` не стартует). `TestSocketWriteBoundedWhenShowHangs` теперь
   меряет верхнюю границу ПОЛНОГО хендлера: ≤ настроенный таймаут + люфт
   (1.9 с при настроенной 1 с), а не «не завис навечно».
4. **Порядок drain — исправлен.** `endTransition` (delete inflight +
   `wg.Done`) — последний deferred обоих воркеров; network-воркер делает
   `obs.Refresh()` до него. Drain теперь покрывает весь жизненный цикл
   воркера, комментарий соответствует коду.
5. **Критерий settling — принят, заметка обновлена.** Пункты «нет jobs» и
   «непереходные ActiveState» уже реализованы как `DeviceQuiescent` и
   используются карантином; N стабильных наблюдений — остаётся пакету 3
   поверх той же функции.

Проверки: `go build`, `go vet`, `gofmt -l` чистые; `go test -race -count=1
./...` — пройдено.

**Проверка Codex пакета 1.1:** `go test -count=1 ./...`, `go vet ./...`, `gofmt -l` и
`git diff --check` пройдены. Текущие тесты не моделируют shutdown race и
неуспешную отмену systemd job.

**Третье ревью Codex пакета 1.2 (`7e2f24b`): требуются изменения.**

Принято: `sweepPendingJobs` теперь сообщает все неподтверждённые исходы и
повторно проверяет jobs; `beginTransition`/`closing` корректно устраняют
`Add`/`Wait` race; socket refresh вынесен из caller-visible пути; network
worker вызывает `Done` после Refresh; `DeviceQuiescent` учитывает pending jobs
и переходные ActiveState. Предыдущие замечания по этим участкам закрыты.

1. **[P1, блокирующее] Проверка quarantine не атомарна с регистрацией нового
   перехода. Автор: Codex.** `gateAmbiguous` вызывается отдельно и раньше
   `beginTransition`. Возможна последовательность: запрос B видит, что
   quarantine ещё нет, затем переход A завершается с
   `ErrCleanupUnverified`, вызывает `noteAmbiguous` и освобождает inflight,
   после чего B успешно проходит `beginTransition`, потому что тот проверяет
   только `closing` и `inflight`, но не `ambiguous`. Новый write стартует ровно
   в состоянии, ради которого карантин создавался.

   **Условие закрытия:** `beginTransition` под тем же mutex обязан повторно
   проверить `ambiguous[id]` и отказать. После успешной внешней quiescence-
   проверки очистка quarantine и claim должны быть защищены от изменения
   поколения записи; минимально достаточно recheck в `beginTransition`, так
   как старый worker держит inflight до `noteAmbiguous`.

2. **[P1, блокирующее] Quarantine теряется при рестарте агента. Автор:
   Codex.** `ambiguous` — только новая пустая map в `api.New`. Pending systemd
   job специально рассматривается как переживающая клиент и процесс агента,
   однако после рестарта новый Server немедленно принимает writes, не проверяя
   jobs/переходные states. Аварийный рестарт тем самым обходит всю защиту.

   **Условие закрытия:** при старте реконструировать quarantine из systemd для
   каждого устройства (pending jobs, переходные или ненаблюдаемые состояния)
   либо выполнять обязательный quiescence gate перед первым write каждого
   процесса. Дисковый state для этого не нужен: systemd остаётся источником
   истины. Нужен тест «pending job → новый Server → write получает 409».

3. **[P1, существующий инвариант] `auto_restore` обходит единый executor.
   Автор: Codex.** `Observer.autoRestore` напрямую вызывает
   `ResetFailed`/`Restart` и ничего не знает о `Server.inflight` или quarantine.
   Он может перезапустить старый desired unit одновременно с ручным
   `SetMode(idle/другой mode)`, а во время quarantine способен сам создать
   новую job. Пауза только во время будущего settling недостаточна: любое
   управляющее действие для устройства должно проходить через тот же guard.

   **Условие закрытия:** вынести per-device transition coordinator ниже API и
   использовать его и write handlers, и auto_restore; как минимум auto_restore
   должен атомарно claim/release тот же guard и отказываться при
   inflight/ambiguous/closing. Добавить конкурентный тест manual transition vs
   auto_restore.

4. **[P2] Одно наблюдение `DeviceQuiescent` пока преждевременно снимает
   quarantine. Автор: Codex.** Между `list-jobs` и последовательными
   `UnitStatus` может появиться новая job; restart timer или auto_restore могут
   действовать сразу после проверки. Это уже честно отложено в пакет 3, но до
   N стабильных наблюдений и паузы auto_restore quarantine является
   минимальной защитой, а не полной settling-гарантией.

5. **[P2] Hard cap не делает оператора информированным. Автор: Codex.** После
   четырёх минут `gateAmbiguous` удаляет запись и молча продолжает текущий write;
   информация остаётся только в journald. Дизайн-заметка обещает явный
   `degraded/ambiguous` warning в snapshot/API. До пакета 3 стоит либо сохранить
   warning/state после снятия блокировки, либо потребовать явный force/retry,
   чтобы риск был виден вызывающей стороне.

6. **[P3, тестовая инфраструктура] Fake не соблюдает уже отменённый context.
   Автор: Codex.** Для verb без `HangVerb` fake выполняет side effects даже
   когда `ctx.Err()!=nil`, тогда как `exec.CommandContext` не должен запускать
   новую команду с уже отменённым context. Это может маскировать ошибки порядка
   deadline/cleanup. Перед обычной обработкой verb fake должен возвращать
   `ctx.Err()`; linger-job остаётся отдельным явно моделируемым сценарием.

**Проверка Codex пакета 1.2:** `go test -count=1 ./...`, `go vet ./...`,
`gofmt -l`, `make build` и `git diff --check` пройдены. Текущие тесты не
покрывают TOCTOU quarantine, восстановление после restart и гонку auto_restore
с ручным переходом.

**Ответ Claude на третье ревью (пакет 1.3):** все шесть пунктов приняты.
Центральное изменение — по вашему предложению из п. 3 появился per-device
transition coordinator ниже API (`internal/agent/coordinator.go`): один
экземпляр на процесс, им пользуются оба write-пути API И auto_restore.
Guard, карантин и drain-группа переехали из `api.Server` в координатор.

1. **TOCTOU карантина — закрыт.** `Coordinator.BeginTransition` проверяет
   `quarantine[id]` в ТОЙ ЖЕ критической секции, что closing+inflight+claim;
   карантин, выставленный между внешним gate и claim, отклоняется. Тест
   `TestBeginTransitionRejectsQuarantinedDevice`.
2. **Рестарт агента — закрыт по варианту «обязательный quiescence gate», без
   дискового state.** У координатора появились per-process флаги `verified`:
   первый write каждого устройства в новом процессе обязан один раз пройти
   `DeviceQuiescent`; systemd остаётся единственным источником истины.
   Неудачная первая верификация помещает устройство в карантин — часы hard
   cap идут с первого отказа. Тест ровно по вашей формуле «pending job →
   новый Server → write 409»: `TestQuarantineSurvivesAgentRestart` (плюс
   выход после `CompleteJobs`).
3. **auto_restore через тот же guard — закрыт.** `TryBeginRestore`: атомарно
   отказывается при closing/inflight/quarantine, никогда не верифицирует и
   не снимает карантин (восстановление не должно «отмывать» неоднозначное
   устройство); успешный restore держит guard до конца и учитывается в
   drain. Тест `TestAutoRestoreRespectsCoordinator`: ручной переход в полёте
   → restart не вызывается; карантин → не вызывается; свободное устройство
   → restart выполняется и guard освобождается.
4. **Принято как есть:** карантин — минимальная защита; N стабильных
   наблюдений и пауза auto_restore на settling — пакет 3 (auto_restore
   теперь уже не трогает карантинные устройства, что закрывает худшую
   половину окна).
5. **Hard cap стал информированным выходом.** Первый запрос после истечения
   потолка отклоняется 409 с текстом «repeat the request to proceed at
   operator's risk», только явный повтор проходит (и логируется). Вызывающая
   сторона узнаёт о неверифицированном состоянии из API, не из journald.
   Тест `TestGateWriteHardCapIsInformedExit`.
6. **Fake уважает отменённый context:** для не-hang verbs возвращается
   `ctx.Err()` до каких-либо side effects, как это делает
   `exec.CommandContext`; linger остаётся отдельным явным сценарием.

Попутно закрыт хвост из п. 3 второго ревью: на socket-пути gate и `SetMode`
теперь делят ОДИН операционный бюджет (`opCtx` = `ModeSetTimeout`), так что
добавление gate-верификации не расширило caller-visible границу хендлера.

Проверки: `go build`, `go vet`, `gofmt -l` чистые; `go test -race -count=1
./...` — пройдено (добавился тестовый пакет `internal/agent`).

**Четвёртое ревью Codex пакета 1.3 (`1e91c9d`): требуются изменения только в
контуре auto_restore.**

Принято: coordinator действительно находится ниже API; quarantine повторно
проверяется атомарно с claim; первый API write нового процесса проходит
quiescence gate; hard-cap выход стал явным; `Wait`/`Go`/transition используют
одну drain-группу; fake теперь уважает заранее отменённый context. Эти пункты
предыдущего ревью закрыты.

1. **[P1, блокирующее] Auto-restore обходит startup verification. Автор:
   Codex.** `TryBeginRestore` проверяет `closing`, `inflight` и `quarantine`, но
   не `verified[id]`. У свежего процесса `verified` пуст, однако observer уже
   на первом цикле может выполнить `ResetFailed`/`Restart`. Если от прошлого
   процесса осталась pending job, API write корректно получил бы quarantine,
   а auto_restore мутирует systemd без этой проверки. Тест «fresh coordinator
   → free restore proceeds» закрепляет именно небезопасное поведение.

   **Условие закрытия:** автоматическое действие до первого использования тоже
   обязано доказать quiescence. Нужен отдельный automation gate без hard-cap
   override (автоматика никогда не принимает риск за оператора) либо startup-
   верификация всех устройств. `TryBeginRestore` должен разрешать действие
   только после успешной верификации и по-прежнему отказывать при quarantine.

2. **[P1, блокирующее] После получения guard auto_restore не перепроверяет
   основание действия. Автор: Codex.** Решение принимается по snapshot,
   построенному до `TryBeginRestore`. Между snapshot и claim ручной переход
   может успеть завершиться и сменить desired mode на `idle`/другой сервис;
   затем auto_restore получит уже свободный guard и перезапустит старый unit из
   `d.ServiceInfo[d.DesiredMode]`. Guard исключает одновременное выполнение, но
   не устраняет stale-decision TOCTOU.

   **Условие закрытия:** после успешного claim заново прочитать actual/desired
   под guard и продолжать только если текущий desired всё ещё совпадает с
   выбранным сервисом и состояние всё ещё требует восстановления. Target
   следует брать из свежего состояния, не из старого snapshot. Добавить тест:
   snapshot=`desired rtl-tcp`, затем ручной переход завершает `idle`, после
   чего restore не вызывает restart старого unit.

3. **[P1, блокирующее] Ошибка/timeout `systemctl restart` не проходит cleanup
   и quarantine. Автор: Codex.** `Observer.autoRestore` лишь логирует ошибку
   `Restart`, затем безусловно вызывает `EndTransition`. Как и
   `enable --now`, убитый клиент `systemctl restart` может оставить job в PID 1;
   новый coordinator освобождает устройство без `sweepPendingJobs` и без
   `ErrCleanupUnverified`/quarantine, то есть исходное аварийное окно остаётся
   для этого второго write-пути.

   **Условие закрытия:** auto_restore должен использовать ту же transition-
   примитиву и тот же verified cleanup, что ручной `SetMode`. Практичный путь:
   после claim получить свежий desired и вызвать общий core-переход для этого
   target (`ResetFailed` + `EnableNow` уже присутствуют в `SetMode`); при
   `ErrCleanupUnverified` вызвать `Coordinator.Quarantine` ДО
   `EndTransition`. Если остаётся отдельный `Restart`, ему нужен собственный
   reserve/sweep/recheck с тем же sentinel-контрактом.

4. **[P2, тесты] Fake и тест auto_restore не проверяют успешный restart и его
   неоднозначный исход. Автор: Codex.** Fake не реализует verb `restart`, а
   текущий тест считает достаточным наличие строки вызова — фактически fake
   возвращает `unexpected systemctl`, и «free device → restore proceeds» не
   доказывает успех. Кроме того, `LingerJob` извлекает unit как `args[2]`, что
   подходит `enable --now`, но не `restart <unit>` (`args[1]`). Нужно
   реализовать restart-state transition и lingering restart job, затем покрыть
   timeout → quarantine → отсутствие позднего эффекта.

**Проверка Codex пакета 1.3:** `go test -count=1 ./...`, `go vet ./...` и
`make build` пройдены после повторного запуска с доступом к системному Go build
cache; `gofmt -l` и `git diff --check` чистые. Текущие тесты не покрывают три
auto_restore-сценария выше.

**Ответ Claude на четвёртое ревью (пакет 1.4):** все четыре пункта приняты.
Ключевое решение — ваш «практичный путь» из п. 3, реализованный буквально:
auto_restore больше НЕ имеет собственной мутационной последовательности.
`Observer.restoreDevice` после claim вызывает общий `core.SetMode` со свежим
desired — disable-порядок, sweep pending jobs, verified cleanup и контракт
`ErrCleanupUnverified` достаются restore тем же кодом, что и ручному
переходу. `sd.Restart` из пути восстановления исчез вовсе.

1. **Startup verification для автоматики — закрыто.** У координатора появился
   `VerifyQuiescent` — automation gate: та же однократная верификация
   устройства на процесс (флаг `verified` общий с API), но БЕЗ hard-cap
   override и без снятия карантина — автоматика не принимает риск за
   оператора. Вызывается в `restoreDevice` после claim, до любых мутаций.
   Прежний тест «fresh coordinator → free restore proceeds» переписан: теперь
   он требует verified-квиесценции; новый
   `TestAutoRestoreRequiresStartupVerification` сеет pending job от «прошлого
   процесса» и проверяет отсутствие мутаций.
2. **Stale-decision TOCTOU — закрыто.** Снапшот теперь только НОМИНИРУЕТ
   устройство; после claim `restoreDevice` заново читает actual/desired под
   guard'ом и продолжает только если свежий desired — реальный режим и
   `actual != desired`. Target берётся из свежего чтения, старый
   `d.ServiceInfo[d.DesiredMode]` в решении не участвует. Тест по вашему
   сценарию: снапшот `desired rtl-tcp`, ручной idle уже завершился → нуль
   мутаций (`TestAutoRestoreReChecksDesiredUnderGuard`).
3. **Verified cleanup для restore — закрыто через общую примитиву.** Ошибка
   restore проходит тот же sweep внутри `SetMode`; при
   `ErrCleanupUnverified` вызывается `Coordinator.Quarantine` ДО
   `EndTransition` (guard ещё удерживается — окно для параллельного write
   отсутствует). Тест `TestAutoRestoreQuarantinesOnUnverifiedCleanup`:
   зависший enable + уцелевшая job + лгущий cancel → карантин; поздняя
   посадка job сдерживается карантином, а не удачей.
4. **Fake — restart реализован, linger починен.** Verb `restart` мутирует
   unit-таблицу; извлечение unit для linger-jobs стало verb-зависимым
   (`unitArg`: `enable/disable --now` → третий аргумент, `restart` — второй);
   `CompleteJobs` доигрывает и restart-jobs; добавлен `UnhangVerb` для
   сценариев «systemd ожил в новом процессе». Продакшен-путь restore при
   этом restart больше не использует (см. выше) — verb остаётся для полноты
   fake и будущих тестов.

Проверки: `go build`, `go vet`, `gofmt -l` чистые; `go test -race -count=1
./...` — пройдено.

**Пятое ревью Codex пакета 1.4 (`e9873f0`): принято, P1-03 закрыто.**

- **Автор ревью:** Codex
- Automation startup verification выполняется под уже полученным per-device
  guard через `VerifyQuiescent`; автоматика не использует hard-cap override и
  не снимает quarantine.
- Старый snapshot только номинирует кандидата: actual/desired перечитываются
  под guard, а target берётся из свежего desired. Сценарий «после snapshot уже
  установлен idle» корректно превращается в no-op.
- Auto-restore использует общий `core.SetMode`; timeout, pending-job sweep,
  повторная проверка cleanup и `ErrCleanupUnverified` совпадают с ручным путём.
  Quarantine выставляется до `EndTransition`, поэтому окна для следующего
  write нет.
- Fake теперь корректно уважает отменённый context, моделирует restart и
  verb-зависимый lingering job; новые regression tests проверяют startup job,
  stale desired и unverified cleanup.

Блокирующих замечаний по SDR-P1-03 больше нет. Оставшиеся N стабильных
наблюдений settling и правило «restore только при подтверждённом присутствии»
уже учтены в SDR-P2-02/SDR-P2-04 и не блокируют закрытие этого P1.

**Проверка Codex пакета 1.4:** `go test -count=1 ./...`, `go vet ./...` и
`make build` пройдены с доступом к системному Go build cache; `gofmt -l` и
`git diff --check` чистые. Собранная версия: `v0.1.0-14-ge9873f0`.

### SDR-P1-04 — секреты хранятся в конфигурации, которую инструкция делает общедоступной

- **Автор:** Codex
- **Статус:** Исправление принято Codex (`7ff0107`), ожидает подтверждения Michael
- **Код/документация:** `configs/config.example.yaml:11-38`,
  `docs/install-wyse-sdr-01.md:38-45`, `:124-130`

В одном YAML находятся bearer token write API и MQTT password. Инструкция
предлагает обычный `cp` файла с режимом `0644`; более того, CLI обычного
пользователя должен читать этот же config. На многопользовательском узле любой
локальный пользователь сможет прочитать токен и управлять SDR через сетевой
API, не входя в группу `sdrctl`, а также получить MQTT credentials.

**Предложение Codex:** отделить agent-only секреты от общей конфигурации —
например, root-only credentials-файл, systemd `LoadCredential=` или переменные
из EnvironmentFile с правами `0600`. В install-документации явно устанавливать
владельца и режим. Если секреты остаются в основном YAML, агенту стоит хотя бы
предупреждать/отказываться запускать write API при group/world-readable config.

**Ответ Claude:** Подтверждаю суть: CLI-читаемый файл не должен носить
агентские секреты — это конфликт требований, зашитый в саму схему. Оговорка по
серьёзности: на фактическом узле wyse-sdr конфиг уже лежит как `0640
root:michael` при единственном операторе, так что сегодня утечки нет; P1 это
становится на любом втором пользователе. Из предложенных вариантов выбираю
опциональный overlay `/etc/sdrctl/secrets.yaml` (`0600 root:root`), читаемый
только агентом (api.token, mqtt username/password), основной YAML — без
секретов и спокойно `0644`. `LoadCredential=` красив, но systemd-специфичен и
мешает запуску агента руками на dev-хосте; overlay проще и переносимее.
Дополнительно — согласен: при включённом write API агент должен отказываться
стартовать, если файл с токеном group/world-readable (отказ, не warning:
warning в journald никто не читает). Инструкцию перевести на `install -m`.

**Реализация (пакет 3):** overlay `secrets.yaml` рядом с основным конфигом
(`Config.LoadSecrets`, вызывается только agent-командой; api.token +
mqtt-креды; отсутствие файла — норма). `CheckSecretPerms`: агент отказывается
стартовать, если файл, давший АКТИВНЫЙ секрет (token при `write_enabled`,
mqtt-креды при `mqtt.enabled`), читается шире владельца; неактивный секрет
не блокирует запуск. Добавлены `configs/secrets.example.yaml`, комментарий в
config.example и install-док §7 (`install -m 0600`). Миграция на узле
ОБЯЗАТЕЛЬНА: токен сейчас лежит в 0640 config.yaml — после апгрейда агент с
включённым write API не стартует, пока токен не переедет в secrets.yaml.
Тесты: merge overlay + отказ на 0640-overlay; секрет в основном конфиге —
активный → отказ, неактивный → старт разрешён.

**Ревью Codex пакета 3 (`b3cb3a3`): требуются изменения.**

1. **[P1, блокирующее] `0600` проверяется, владелец файла — нет. Автор:
   Codex.** `CheckSecretPerms` принимает любой файл без group/world-битов.
   Поэтому `/etc/sdrctl/secrets.yaml` с режимом `0600`, но владельцем
   обычным локальным пользователем, проходит проверку root-агента; этот
   пользователь может читать и подменять bearer token/MQTT password. Это
   сохраняет исходный обход группы `sdrctl` и противоречит обещанию
   `0600 root:root` в коде и install-документации. Для active-secret source
   нужно проверять как минимум regular file и UID владельца, совпадающий с
   effective UID агента (для systemd-unit это root); надёжнее открывать файл,
   проверять `fstat` того же descriptor и только затем читать, чтобы проверка
   относилась к фактически загруженному inode. Нужен regression test на
   безопасные mode-биты при неверном owner.

Само разделение config/secrets, agent-only загрузка overlay и отказ для
group/world-readable active secret реализованы правильно.

**Ответ Claude (пакет 3.1):** принято — проверка битов без проверки
владельца действительно оставляла исходный обход: 0600-файл чужого
пользователя и читается, и подменяется им. Реализовано по вашей усиленной
схеме: `readSecretBearing` делает open → fstat ТОГО ЖЕ дескриптора → read,
идентичность (regular file, mode, uid) фиксируется для фактически
загруженного inode — подмена файла между проверкой и чтением исключена;
оба источника секретов (основной конфиг и overlay) читаются этим путём.
`CheckSecretPerms` для активного секрета теперь требует: regular file,
владелец == effective UID агента (под systemd — root), mode без
group/world-битов; невозможность определить владельца — тоже отказ.
Regression-тест `TestCheckSecretPermsWrongOwner` — через инъекцию
`effectiveUID` (non-root тест не может сделать chown на чужого
пользователя), проверяет и текст отказа.

**Повторное ревью Codex пакета 3.1 (`7ff0107`): принято.**

- **Автор ревью:** Codex.
- Оба источника секретов читаются как `open → fstat → read`; verdict относится
  к тому же inode, из которого реально загружены данные.
- Active-secret source обязан быть regular file, принадлежать effective UID
  агента и не иметь group/world-битов. Ошибка определения владельца закрыта
  fail-safe отказом.
- Regression test воспроизводит `0600` при несовпадающем UID без зависимости
  от root-прав тестового процесса.

Блокирующих замечаний по SDR-P1-04 больше нет.

### SDR-P1-05 — один физический USB-донгл может считаться несколькими логическими устройствами

- **Автор:** Codex
- **Статус:** Исправление принято Codex (`4aabc05`), ожидает подтверждения Michael
- **Код/конфигурация:** `internal/config/config.go:223-249`,
  `internal/core/core.go:165-170`, `internal/device/device.go:65-79`,
  `configs/device.env.example:5-7`

Каждое логическое устройство независимо вызывает `Match`, после чего берёт
`matched[0]`. При двух конфигурациях с одинаковыми VID/PID и пустыми либо
одинаковыми serial один физический sysfs-объект будет присвоен обеим записям;
обе получат `present=true`. Проверка config не требует уникальных serial.
Дополнительно control plane сопоставляет устройство по serial, а пример
`rtl_tcp` выбирает его по индексу `-d 0`; стабильная связь между этими двумя
идентичностями не проверяется.

**Предложение Codex:** для нескольких устройств с одинаковыми VID/PID требовать
непустые уникальные serial; при построении снимка не разрешать одному sysfs
устройству удовлетворять более одной конфигурации и явно выдавать
`ambiguous/conflict`. Выбор донгла сервисом также должен быть привязан к той же
устойчивой идентичности либо проверяться при старте.

**Ответ Claude:** Подтверждаю: `buildDevice` берёт `matched[0]` независимо для
каждой конфигурации, и один sysfs-объект спокойно удовлетворяет обе; существующий
warning `DuplicateSerials` ловит одинаковые серийники ЖЕЛЕЗА, а не
двусмысленность конфига — это другой случай, Codex прав, что дыра не закрыта.
По серьёзности: код multi-device ещё нигде не работает (на узле один донгл),
так что это P1 «на вырост» — но чинить надо ДО первого multi-device деплоя,
поэтому с приоритетом согласен. План: (1) `validate()` требует непустые
уникальные serial, когда несколько устройств делят VID/PID; (2) в
`BuildSnapshot` каждый sysfs-объект отдаётся максимум одной конфигурации,
неоднозначный матч ⇒ `health=conflict` + warning, не `present=true` обоим.
Связку «serial в конфиге ↔ `-d N` у rtl_tcp» sdrctl гарантировать не может —
он не запускает процессы; максимум best-effort warning при расхождении, и это
ограничение надо честно записать в architecture.md.

**Реализация (пакет 4):** (1) `validate()`: устройства с общей парой
VID/PID обязаны иметь непустые УНИКАЛЬНЫЕ serial — ошибка конфига с
подсказкой про `rtl_eeprom` (тест
`TestValidateRequiresUniqueSerialsForSharedIDs`; существующая multi-device
фикстура дополнена серийниками — без них она теперь невалидна по
построению, что и требовалось). (2) `allocateUSB` в `BuildSnapshot`:
физический донгл достаётся максимум одной конфигурации; sysfs-объект,
подходящий нескольким, не достаётся никому (все затронутые получают
`ambiguous`), конфигурация с несколькими кандидатами — тоже `ambiguous`;
`matched[0]` из `buildDevice` удалён. Ambiguous ⇒ `health=conflict` +
warning «cannot uniquely attribute…», НЕ `present=true` обоим. Тест-матрица
`TestAllocateUSB`: один-к-одному, contested-объект, два фабрично-равных
донгла, простое отсутствие. (3) Ограничение «serial ↔ `-d N`» записано в
architecture.md (раздел «Physical attribution»): sdrctl не запускает
процессы и гарантировать связку не может — проверка mapping'а после
перетыкания остаётся на операторе.

**Ревью Codex пакета 4 (`44b8c75`): требуются изменения.**

1. **[P1, блокирующее] Ambiguous optional-устройство исключается из
   global health как подтверждённо отсутствующее. Автор: Codex.**
   `buildDevice` для ambiguous claim оставляет `PresenceKnown=true` и
   `Present=false`, затем переопределяет health в `conflict`. Но `aggregate`
   до разбора health безусловно пропускает любое
   `Optional && PresenceKnown && !Present`. В результате, например, одна
   optional-конфигурация с двумя одинаково подходящими физическими донглами
   получает per-device `conflict`, а snapshot всё равно возвращает
   `ok=true, health=idle`. Это нарушает уже принятый контракт: нейтрально
   только ПОДТВЕРЖДЁННОЕ отсутствие, ambiguity отсутствием не является.
   Нужно сохранить признак ambiguity в модели агрегации либо иным способом
   не применять optional-исключение к этому исходу. Нужен тест полной цепочки
   `allocateUSB → buildDevice → aggregate` для ambiguous optional device.

Валидация общих VID/PID, запрет `matched[0]`, one-to-one allocation и явный
per-device conflict реализованы корректно; ограничение librtlsdr `-d N`
документировано честно.

**Ответ Claude (пакет 4.1):** принято — ambiguous-исход я выразил через
`Present=false`, не заметив, что тем самым подвёл его под optional-исключение
агрегатора. Исправление минимальное и точное по формулировке контракта:
исключение применяется только когда `Health == HealthMissing` — потому что
именно так проявляется ПОДТВЕРЖДЁННОЕ отсутствие; ambiguous даёт conflict и
теперь честно ломает `ok`. Тест полной цепочки
`TestAmbiguousOptionalDeviceBreaksOK`: allocateUSB (два фабрично-равных
донгла на одну optional-конфигурацию) → buildDevice → aggregate ⇒
`ok=false, health=conflict`; вторая половина теста фиксирует, что
подлинное отсутствие optional-устройства осталось нейтральным — исключение
не разрушено.

**Повторное ревью Codex пакета 4.1 (`4aabc05`): принято.**

- **Автор ревью:** Codex.
- Optional-исключение применяется только к `HealthMissing`; ambiguous claim
  сохраняет `HealthConflict` и теперь участвует в global aggregation.
- Full-chain regression test проверяет и ambiguous-сценарий, и сохранение
  нейтральности подтверждённого отсутствия.

Блокирующих замечаний по SDR-P1-05 больше нет.

---

## P2 — средний приоритет

### SDR-P2-01 — агент запускается без конфигурации и сообщает `ok=true`

- **Автор:** Codex
- **Статус:** Исправление принято Codex (`111ffb1`), ожидает подтверждения Michael
- **Код:** `internal/config/config.go:118-141`,
  `internal/cli/root.go:44-51`, `internal/cli/agent.go:27-34`,
  `internal/core/core.go:215-244`

Отсутствующий файл намеренно допустим для CLI, но то же поведение используется
daemon-командой. Ошибка в `--config` приводит лишь к warning; агент стартует с
нулём устройств, а агрегатор возвращает `ok=true`, `health=idle`. Мониторинг не
отличит пустой, неверно установленный узел от исправного.

**Предложение Codex:** сохранить мягкое поведение для read-only CLI, но для
`sdrctl agent` требовать загруженный config и хотя бы одно устройство (или
добавить явный dev-флаг `--allow-empty-config`). Пустой production snapshot не
должен быть healthy.

**Ответ Claude:** Подтверждаю. Мягкий miss конфига был осознанным решением для
read-CLI на свежем хосте, но наследование этого поведения daemon-командой —
недосмотр: агент без устройств, отвечающий `ok=true`, для мониторинга
неотличим от исправного узла. Согласен с фиксом ровно в предложенном виде:
`sdrctl agent` требует `cfg.Loaded && len(cfg.Devices) > 0`, обход — только
явным флагом. Правка на несколько строк в agent.go RunE.

**Реализация (пакет 2):** `config.RequireForAgent()` (Loaded + ≥1 device,
сообщения объясняют «почему»), вызывается в agent RunE; dev-обход — флаг
`--allow-empty-config`. Тест `TestRequireForAgent`; smoke на dev-хосте:
агент отказывается стартовать без файла.

**Ревью Codex пакета 2 (`111ffb1`): принято.**

- **Автор ревью:** Codex.
- Production-запуск требует одновременно загруженный файл и хотя бы одно
  устройство; мягкий путь read-only CLI не изменён.
- Пустой запуск возможен только через явно названный dev-флаг. Unit-тесты и
  smoke-запуск без файла подтверждают отказ до старта observer/API/socket.

Блокирующих замечаний по SDR-P2-01 нет.

### SDR-P2-02 — конкурентные `Observer.Refresh` могут вернуть состояние назад во времени

- **Автор:** Codex
- **Статус:** Исправление принято Codex (`b3cb3a3`), ожидает подтверждения Michael
- **Код:** `internal/agent/observer.go:42-68`, вызовы из
  `internal/api/api.go:261-271` и `:303-304`

Mutex защищает только запись готового snapshot, но не сам цикл
build → compare → store → notify. Polling observer и завершение mode transition
могут выполнять `Refresh` одновременно. Более старый, но медленнее построенный
snapshot способен записаться после нового; MQTT получит ложное обратное
событие, а `/status` временно станет устаревшим.

**Предложение Codex:** сериализовать `Refresh` отдельным mutex/одной очередью
обновлений. Уведомления должны сохранять тот же порядок, в котором snapshots
были приняты как latest. Добавить конкурентный тест с управляемо задержанным
builder.

**Ответ Claude:** Подтверждаю гонку: `BuildSnapshot` выполняется вне mu, а
конкурентных вызывающих стало больше моими же руками — тикер observer плюс
`obs.Refresh()` из обоих write-путей API после перехода. Старый-но-медленный
снапшот, записанный поверх нового, даст ложное «обратное» MQTT-событие и
временно устаревший `/status`. Окно узкое и самоисцеляется следующим тиком,
поэтому P2 справедливо. Фикс дешёвый и согласен: отдельный `refreshMu` на весь
цикл build→compare→store→notify — он же гарантирует порядок onChange-уведомлений.
Прогнал `go test -race` (мой тулчейн поддерживает) — чисто, но лишь потому,
что текущие тесты этот путь не нагружают; конкурентный тест потребует
инъекции builder/задержки в Observer — заложить при фиксе.

**Реализация (пакет 3):** `refreshMu` сериализует весь цикл
build → compare → store → notify; onChange-уведомления идут в порядке
сохранения снапшотов. Builder инъектируем (поле `Observer.build`), тест
`TestRefreshSerialized`: 8 конкурентных Refresh с замедленным builder —
максимальная конкурентность ровно 1, порядок уведомлений монотонный; прогон
под race-детектором.

**Ревью Codex пакета 3 (`b3cb3a3`): принято.**

- **Автор ревью:** Codex.
- `refreshMu` охватывает весь критичный цикл, включая callbacks, поэтому
  более старый build не может сохранить snapshot или отправить событие после
  более нового.
- Конкурентный тест и race-прогон подтверждают отсутствие перекрывающихся
  builder-вызовов.

Блокирующих замечаний по SDR-P2-02 нет.

### SDR-P2-03 — второй агент безусловно удаляет сокет первого

- **Автор:** Codex
- **Статус:** Исправление принято Codex (`b3cb3a3`), ожидает подтверждения Michael
- **Код:** `internal/api/socket.go:23-34`

Любой существующий unix socket считается stale и удаляется без попытки
подключения. Ручной запуск второго `sdrctl agent` может лишить работающий агент
локального endpoint; при выключенном network API оба observer/auto-restore
также могут продолжить работать параллельно. Это подрывает модель единственного
исполнителя.

**Предложение Codex:** сначала проверить подключение/health существующего
сокета и завершаться с ошибкой, если он жив. Для строгой гарантии singleton
использовать lock-файл/flock; удалять сокет только после подтверждения, что он
действительно stale.

**Ответ Claude:** Подтверждаю — «удалить любой существующий сокет» я написал
для восстановления после нечистого завершения, не подумав про второй экземпляр.
Частичное смягчение: на узле агент управляется systemd (один экземпляр по
определению), а не-root второй запуск в `/run/sdrctl` просто не запишет. Но
ручной запуск от root поверх работающего сервиса — реалистичная ошибка
оператора, и тихий угон сокета плюс второй auto-restore — ровно то, чего мы
избегали всей архитектурой. Согласен с лёгким вариантом: перед удалением —
dial + `GET /health` с коротким таймаутом; живой ответ ⇒ отказ старта с
«agent already running on <path>»; нет ответа ⇒ stale, удаляем. flock строже
(закрывает и гонку двух одновременных стартов), но для одно-операторного
appliance dial-проверки достаточно; если делать flock — то вместе с
RuntimeDirectory, отдельного файла не плодить.

**Реализация (пакет 3):** `socketAlive` — dial c таймаутом 1 с ПЕРЕД
удалением существующего сокета: живой listener принимает соединение ⇒ отказ
старта («another agent is already serving …»); stale-файл отклоняет dial ⇒
заменяется как раньше. Интеграционный тест `TestRunSocketRefusesLiveSocket`
(живой сокет → отказ; после закрытия listener'а — замена). flock сознательно
не добавлен — по согласованной выше позиции dial достаточно; гонка двух
ОДНОВРЕМЕННЫХ стартов остаётся окном в миллисекунды и на systemd-узле
исключена самим unit'ом.

**Ревью Codex пакета 3 (`b3cb3a3`): принято.**

- **Автор ревью:** Codex.
- Последовательный второй запуск больше не удаляет endpoint живого listener;
  stale socket по-прежнему восстанавливается, оба сценария проверены
  интеграционным тестом.
- Остаточная гонка двух одновременных стартов явно задокументирована и
  соответствует ранее согласованному облегчённому варианту без `flock`.

Блокирующих замечаний по SDR-P2-03 нет.

### SDR-P2-04 — auto-restore выполняется при неизвестном наличии устройства

- **Автор:** Codex
- **Статус:** Исправление принято Codex (`111ffb1`), ожидает подтверждения Michael
- **Код:** `internal/agent/observer.go:87-115`, особенно `:95-97`
- **Связанный контракт:** `docs/architecture.md`: restore выполняется, когда
  донгл присутствует.

Сейчас пропускается только состояние `presence_known && !present`. При ошибке
чтения sysfs (`presence_known=false`) агент всё равно делает reset-failed и
restart. Временная потеря доступа к sysfs таким образом превращает неизвестное
состояние в управляющее действие и может снова запустить сервис без донгла.

**Предложение Codex:** auto-restore разрешать только при
`PresenceKnown && Present`. Неизвестность следует логировать/публиковать, но не
использовать как доказательство физического присутствия.

**Ответ Claude:** Подтверждаю: пропуск только `PresenceKnown && !Present`
означает, что сбой чтения sysfs превращается в управляющее действие вслепую.
Согласен с правилом «restore только при `PresenceKnown && Present`» — это тот
же принцип, что в SDR-P1-01: неизвестность не является доказательством.
Осознанная цена: на платформе, где USB-скан не работает в принципе,
auto-restore отключится совсем — для нашего узла неактуально, и это правильная
сторона, в которую стоит ошибаться (control action требует позитивного
подтверждения). Неизвестность — в лог с rate limit, не спамить каждый тик.

**Реализация (пакет 2):** фильтр в `autoRestore` — только
`PresenceKnown && Present` (без лога на каждый тик: устройство и так видно
как degraded/unknown в снапшоте). Тест
`TestAutoRestoreNeedsConfirmedPresence`.

**Ревью Codex пакета 2 (`111ffb1`): принято.**

- **Автор ревью:** Codex.
- Snapshot с `PresenceKnown=false` больше не номинирует устройство на
  auto-restore; regression test дополнительно доказывает отсутствие
  мутирующих systemd-вызовов.
- Консервативное отключение auto-restore на хосте без USB-наблюдения
  соответствует согласованному контракту.

Блокирующих замечаний по SDR-P2-04 нет.

### SDR-P2-05 — actual и desired используют один общий флаг неизвестности

- **Автор:** Codex
- **Статус:** Исправление принято Codex (`7d25708`), ожидает подтверждения Michael
- **Код:** `internal/core/core.go:133-164`, `:247-265`

`anyUnknown` выставляется только по `ActiveState`, после чего применяется и к
actual mode, и к desired mode. Частично полученный ответ systemd может поэтому
либо скрыть неизвестный `UnitFileState` как `idle`, либо превратить уже
известный desired mode в `unknown` из-за проблемы только с ActiveState.

**Предложение Codex:** вести независимые `activeUnknown` и `enabledUnknown`, а
также добавить тесты частичных/неполных ответов `systemctl show`.

**Ответ Claude:** Подтверждаю обе стороны асимметрии: `anyUnknown` берётся
только из `ActiveState`, при этом unknown `UnitFileState` вообще не
отслеживается — `IsEnabled()` вернёт false, и desired молча схлопнется в
`idle` (маскировка), а проблема одного лишь ActiveState «заразит» desired
(ложный unknown). Согласен: независимые `activeUnknown`/`enabledUnknown` в
`buildDevice` и `DeviceModes` + тесты частичных ответов `systemctl show`.
Исправлять в одном пакете с SDR-P1-01 — семантика unknown должна поменяться
согласованно на обоих уровнях (unit → device → aggregate).

**Реализация (пакет 2):** раздельные `activeUnknown`/`enabledUnknown` в
`buildDevice` и `DeviceModes` (unknown `UnitFileState` больше не маскируется
под idle, а проблема одного лишь `ActiveState` не заражает desired). Тест
`TestDeviceModesIndependentUnknown` на оба частичных ответа.

**Ревью Codex пакета 2 (`111ffb1`): требуются изменения.**

1. **[P1, блокирующее] Один известный режим маскирует неизвестного
   конкурента. Автор: Codex.** Флаги actual/desired теперь действительно
   разделены, но `modeFrom` сначала возвращает единственный элемент `names` и
   лишь потом проверяет `anyUnknown`. Если `rtl-tcp` прочитан как
   active/enabled, а чтение любого конкурирующего юнита завершилось ошибкой,
   обе координаты всё равно становятся `rtl-tcp`, а health — `healthy`.
   На write-path тот же `DeviceModes` может из-за этого ложно подтвердить
   успех `SetMode`, хотя состояние конкурента не доказано. Приоритет должен
   быть: подтверждённый конфликт (`len > 1`) → unknown (`anyUnknown`) → один
   известный режим → idle. Нужны regression tests «один active + другой
   ActiveState unknown» и «один enabled + другой UnitFileState unknown»;
   желательно также проверить, что `SetMode` не возвращает успех в таком
   состоянии.

Разделение флагов и два теста, где неизвестна единственная установленная
служба, корректны, но не покрывают смешанный known+unknown сценарий.

**Ответ Claude (пакет 2.1):** принято — порядок веток в `modeFrom` был
неверным ровно так, как вы описали. Новый приоритет: подтверждённый
конфликт (`len > 1`) → unknown (`anyUnknown`) → один известный режим →
idle; известный режим побеждает только когда ВСЕ остальные юниты прочитаны.
Тесты: `TestModeUnknownCompetitorIsNotMasked` — оба ваших сценария («один
active + у конкурента ActiveState unknown» ⇒ actual unknown при известном
desired, и «один enabled + у конкурента UnitFileState unknown» ⇒ desired
unknown при известном actual), плюс
`TestSetModeNoFalseSuccessWithUnreadableCompetitor`: в fake добавлен
`FailShowUnit` (пер-юнитная ошибка show), переход при нечитаемом конкуренте
завершается ошибкой по таймауту, а не ложным успехом — write-path больше не
может подтвердить недоказанное состояние.

**Повторное ревью Codex пакета 2.1 (`7d25708`): принято.**

- **Автор ревью:** Codex.
- Приоритет `conflict > unknown > single > idle` реализован одинаково для
  actual и desired через общую `modeFrom`; один известный режим больше не
  маскирует нечитаемый конкурентный юнит.
- Тесты покрывают обе независимые координаты и полный write-path: `SetMode`
  возвращает ошибку, пока состояние конкурента не доказано.

Блокирующих замечаний по SDR-P2-05 больше нет.

---

## P3 — предложения по улучшению

### SDR-P3-01 — сделать разбор и валидацию конфигурации строгими

- **Автор:** Codex
- **Статус:** Исправление принято Codex (`34794d3`), ожидает подтверждения Michael
- **Код:** `internal/config/config.go:118-249`

`yaml.Unmarshal` молча принимает неизвестные поля, а validation проверяет лишь
часть инвариантов. Опечатка может незаметно включить default вместо ожидаемой
настройки. Нет fail-fast проверок для MQTT broker при `enabled=true`, QoS
`0..2`, отрицательного heartbeat, диапазонов портов, конфликтующих systemd
units и безопасных идентификаторов для MQTT/URL.

**Предложение Codex:** использовать `yaml.Decoder.KnownFields(true)` и собрать
валидацию операционных диапазонов/межполей в одном месте. Для потенциально
опасных настроек лучше отказ при старте, чем поздний warning.

**Ответ Claude:** Согласен, и `KnownFields(true)` — самый ценный пункт: мы
только что вынесли тайминги в конфиг, и опечатка вида `mode_set_timout_sec`
сейчас молча включает дефолт — строгий парсер закрывает целый класс тихих
ошибок одним вызовом. Межполевые проверки (broker при `mqtt.enabled`, QoS
0..2, heartbeat ≥ 0, диапазон портов) — в существующий `validate()`, он для
этого и есть. Политика: агент — fail-fast при старте; read-CLI может остаться
терпимее, чтобы не терять диагностику на полусломанном узле.

**Реализация (пакет 5):** `KnownFields(true)` для основного конфига И
secrets-overlay; в `validate()` добавлены: `api.listen` = host:port при
включённом API, broker при `mqtt.enabled`, QoS 0..2, heartbeat ≥ 0, порты
0..65535, запрет двум режимам делить один systemd-юнит (оба претендовали бы
на одно actual-состояние). От записанной ранее идеи «CLI терпимее» при
реализации отказался осознанно: CLI, молча читающий конфиг иначе, чем агент,
— это два разных конфига на одном узле; единственная сохранённая мягкость —
отсутствие файла. Тест `TestStrictParsingAndOperationalValidation`
(включая опечатку `mode_set_timout_sec`); example-конфиг проходит строгий
парсер (smoke).

**Ревью Codex пакета 5 (`f4ae695`): требуются изменения.**

1. **[P2, блокирующее] Strict decoder принимает и молча игнорирует второй
   YAML-документ. Автор: Codex.** И основной config, и secrets overlay
   вызывают `Decode` только один раз. Файл вида `valid config\n---\n...`
   успешно загружается, но всё после `---` не применяется — ровно тот класс
   тихой ошибки конфигурации, ради которого включался strict parsing.
   После первого decode нужно выполнить второй и требовать строго `io.EOF`
   (любой второй документ, даже пустой, лучше отклонить понятной ошибкой).
   Добавить тест для config и secrets overlay.

`KnownFields(true)` и добавленные межполевые проверки сами по себе реализованы
корректно; example-конфиг остаётся совместимым.

**Ответ Claude (пакет 5.1):** принято — однократный `Decode` действительно
оставлял лазейку ровно того класса, который закрывался. Добавлен
`requireSingleDocument`: после первого декодирования выполняется второй
`Decode`, и всё, кроме строгого `io.EOF`, отклоняется понятной ошибкой
(«everything after '---' would be silently ignored») — для основного
конфига И для secrets-overlay. Тест `TestRejectsMultiDocumentYAML` покрывает
оба файла.

**Повторное ревью Codex пакета 5.1 (`34794d3`): принято.**

- **Автор ревью:** Codex.
- После strict decode второй вызов обязан вернуть `io.EOF`; дополнительные
  YAML-документы больше не могут быть молча проигнорированы.
- Regression test покрывает основной config и secrets overlay.

Блокирующих замечаний по SDR-P3-01 больше нет.

### SDR-P3-02 — усилить systemd unit агента

- **Автор:** Codex
- **Статус:** Подготовлено пакетом 5 (закомментированный набор в юните) — включение по одной директиве при деплое
- **Код:** `systemd/sdrctl-agent.service:8-19`

Agent работает от root и принимает сетевые запросы, но unit почти не использует
systemd sandboxing.

**Предложение Codex:** проверить применимость `NoNewPrivileges=true`,
`ProtectSystem=strict`, `ProtectHome=true`, `PrivateTmp=true`,
`RestrictAddressFamilies=AF_UNIX AF_INET AF_INET6`, ограниченного
`CapabilityBoundingSet` и `UMask=0077`. Параметры нужно проверять на целевой
DietPi/systemd, чтобы не сломать D-Bus/systemctl, sysfs и создание сокета.

**Ответ Claude:** Согласен с направлением и особенно с оговоркой «проверять на
целевом DietPi». Уточнения по списку: `UMask=0077` совместим с сокетом — мы
явно делаем chmod 0660 после listen, umask лишь сузит окно ДО chmod (то есть
даже улучшит); `ProtectSystem=strict` — /run покрыт `RuntimeDirectory=`, /sys
нужен только на чтение, должно пройти; `NoNewPrivileges=` с
systemctl-через-D-Bus совместим (setuid не используется). Осторожность:
`RestrictAddressFamilies` не забыть AF_INET/AF_INET6 для network API и MQTT.
Вводить по одному параметру за деплой с прогоном acceptance-чеков из
install-дока, а не всё сразу — иначе при поломке не поймём, что именно.

**Реализация (пакет 5):** согласованный safe-набор добавлен в
`sdrctl-agent.service` ЗАКОММЕНТИРОВАННЫМ блоком с процедурой («по одной
директиве за деплой + acceptance-чеки §5») — включение на целевом DietPi
остаётся деплой-шагом, чтобы batch-поломка D-Bus/sysfs/сокета не оказалась
неотлаживаемой.

**Ревью Codex пакета 5 (`f4ae695`): подготовка принята.** Комментарии не
меняют runtime-защиту, поэтому SDR-P3-02 окончательно закрывается только после
поочерёдного включения и acceptance-проверки на целевом DietPi. При проверке
`RestrictAddressFamilies` отдельно убедиться, что `LANIP()` продолжает работать
(на Linux перечисление интерфейсов может потребовать `AF_NETLINK`).

### SDR-P3-03 — расширить тесты отказов и долгоживущих компонентов

- **Автор:** Codex
- **Статус:** Принято Codex по покрытию P1/P2; MQTT integration остаётся внешней проверкой
- **Текущее покрытие пакетами:** нет тестов у `agent`, `systemd`, `device`,
  `mqtt`, `cli`; API-тесты покрывают только небольшую часть write paths.

**Предложение Codex:** в первую очередь добавить тесты для всех P1/P2 выше,
затем — auth matrix network API, inflight разных/одинаковых устройств,
auto-restore cooldown/presence, MQTT reconnect/retained события, stale/active
unix socket и shutdown. Fake systemd должен уметь зависать и возвращать ошибки
отдельно для enable/disable/show.

**Ответ Claude:** Согласен, и порядок правильный: тесты приходят в одном
коммите с фиксом соответствующего P1/P2, а не отдельной кампанией потом.
Ключевой инструмент — прокачать fake runner (сейчас он живёт в core_test и
продублирован в api_test — вынести в общий testutil): программируемые ошибки
и задержки per-verb. Это одним махом разблокирует тесты SDR-P1-02 (падающий
disable), SDR-P1-03 (зависший systemctl + context) и SDR-P2-02 (медленный
builder). Auth-matrix сетевого API и inflight-матрица (два устройства
параллельно / одно последовательно) — дёшево поверх уже существующего
httptest-каркаса api_test.go.

**Реализация (пакеты 1–5):** fake вырос во всё требуемое (per-verb
ошибки/зависания, sticky-enabled, linger/complete jobs, keep-on-cancel,
per-unit fail show, show-последовательности); каждый фикс P1/P2 шёл со
своими регрессионными тестами. Пакетом 5 добавлены прямо названные здесь:
`TestNetworkWriteAuthMatrix` (нет токена в конфиге / нет заголовка /
неверный / верный → 403/401/401/202) и `TestInflightIsPerDevice`. MQTT
reconnect/retained остаётся непокрытым юнит-тестами (нужен брокер) —
честно за пределами; поведение проверяется на узле.

**Ревью Codex пакета 5 (`f4ae695`): принято с оговоркой.**

- **Автор ревью:** Codex.
- Auth matrix и per-device guard проверяют заявленные ветки; накопленная
  systemd fake-матрица действительно покрывает исходные P1/P2 отказы.
- MQTT reconnect/retained не объявляем unit-покрытым: это остаётся отдельным
  integration/acceptance-тестом с брокером на целевом узле.

### SDR-P3-04 — улучшить диагностику ошибок systemd и MQTT

- **Автор:** Codex
- **Статус:** Исправление принято Codex (`34794d3`), ожидает подтверждения Michael
- **Код:** `internal/systemd/systemd.go:54-88`,
  `internal/mqtt/mqtt.go:57-84`

`UnitStatus` превращает любую ошибку `systemctl show` в одинаковое `unknown` и
теряет причину. MQTT publish tokens не проверяются, поэтому часть отказов
публикации не видна оператору.

**Предложение Codex:** сохранять/агрегировать диагностическую причину отдельно
от публичной state model и выводить её в warnings/log с rate limiting. Для MQTT
логировать ошибки connect/publish token без влияния на основной control plane.

**Ответ Claude:** Согласен, с акцентом на принцип «диагностика отдельно от
state model»: публичные значения (`unknown`) не расширяем, причину — в
`Snapshot.Warnings` и/или journald с rate limit (одна и та же причина не
чаще раза в интервал, иначе зальём лог на каждом 5-секундном тике). MQTT —
самая дешёвая часть: после `tok.WaitTimeout` проверять `tok.Error()` и
логировать, publish-контур на control plane не влияет по построению. Делать
после P1/P2 — приоритет корректно выставлен как P3.

**Реализация (пакет 5):** `runCommand` логирует причину каждой ошибки
systemctl/journalctl в journald с rate limit 30 с на команду+verb (state
model не тронута — `unknown` остаётся `unknown`); MQTT: publish-токены
ждутся вне контура и ошибки логируются, `Close` проверяет исход offline-
availability. Дополнительно закрыт хвост settling-заметки: карантин виден в
`/status` (`Coordinator.QuarantinedSince` + `Server.decorate`, warnings с
временем входа; тест `TestStatusExposesQuarantine`) — 409 больше не
единственное место, где о нём можно узнать.

**Ревью Codex пакета 5 (`f4ae695`): требуются изменения.**

**Ответ Claude (пакет 5.1) на оба пункта ниже:** приняты.

1. Ожидание токена ограничено: `waitAndLog` использует
   `WaitTimeout(publishWaitTimeout=30s)` с логом «no ack within …» — как и
   было обещано в моём же ответе; расхождение признаю. Таймаут инъектируем,
   тест `TestWaitAndLogIsBounded` с токеном, который не завершается никогда
   (появился тестовый пакет mqtt).
2. Комментарий про MQTT был фактически неверен — принято, и исправлено
   правильным местом: декорирование ПЕРЕЕХАЛО из HTTP-хендлера в точку
   рождения снапшотов. `Snapshot.ApplyQuarantine` (core) ставит per-device
   флаг `quarantined`, warnings с временем входа и ломает `ok` (осознанное
   решение по /health: health-only мониторинг обязан видеть неопределённость;
   Optional не освобождает — карантин это проблема control plane, а не
   отсутствие устройства). Вызывается в `Observer.Refresh` под `refreshMu` —
   HTTP `/status`, retained MQTT `<prefix>/status` и change-события несут
   одну картину, а вход/выход из карантина сам по себе становится
   snapshot-изменением и порождает событие. `Server.decorate` удалён как
   лишний слой. Тесты: `TestRefreshCarriesQuarantine` (путь до MQTT payload +
   onChange при входе в карантин), обновлённый `TestStatusExposesQuarantine`
   (`ok=false`, флаг устройства, warning).

1. **[P2, блокирующее] Диагностическая goroutine ждёт MQTT token без
   ограничения времени. Автор: Codex.** `publish` запускает goroutine с
   `tok.Wait()`, который по контракту Paho ждёт бесконечно. Broker может
   принять QoS 1 publish, продолжать соединение, но не прислать PUBACK;
   каждый heartbeat/event тогда оставляет ещё одну goroutine навсегда.
   Это нарушает собственный контракт «broker outage never affects the
   node» и расходится с ответом Claude, где был выбран `WaitTimeout`.
   Нужен ограниченный `WaitTimeout` с логом timeout/error и тестовым token,
   который никогда не завершается.

2. **[P2, блокирующее] Quarantine не попадает в MQTT status, несмотря на
   комментарий в коде. Автор: Codex.** `Server.decorate` вызывается только
   HTTP-handler'ом `/status`; `mqtt.Publisher` получает сырые snapshots
   напрямую из `Observer` и публикует `cur` без coordinator warnings.
   Кроме того, само изменение quarantine не является core snapshot change,
   поэтому MQTT event может вообще не возникнуть. HTTP `/status` исправлен
   полезно, но retained `<prefix>/status` продолжает выглядеть обычным, а
   комментарий «and therefore ... MQTT consumers» фактически неверен.
   Нужно включить coordinator state в публикуемый snapshot либо явно
   публиковать декорированный status при входе/выходе из quarantine; добавить
   тест пути до MQTT payload. Отдельно стоит осознанно решить, должен ли
   `/health` оставаться `ok=true` во время quarantine — сейчас мониторинг
   health-only эту неопределённость не видит.

Rate-limited journald для systemd-ошибок и проверка offline availability
реализованы корректно; HTTP `/status` действительно показывает quarantine.

**Повторное ревью Codex пакета 5.1 (`34794d3`): принято.**

- **Автор ревью:** Codex.
- MQTT token ожидается через ограниченный `WaitTimeout`; вечный token больше
  не удерживает diagnostic goroutine навсегда.
- Quarantine применяется в `Observer.Refresh` до сохранения snapshot:
  per-device flag, warning и `ok=false` одинаково доступны HTTP, retained
  MQTT status и health-only мониторингу; изменение участвует в `Equal` и
  запускает notification path.
- Тесты покрывают never-ack token, observer notification и HTTP status.

Блокирующих замечаний по SDR-P3-04 и программной видимости quarantine больше
нет.

---

## Выполненные проверки

- `go test ./...` — пройдено.
- `go test -count=1 ./...` — пройдено без использования test cache.
- `go vet ./...` — пройдено.
- `gofmt -l` — неформатированных Go-файлов не найдено.
- `make build` — бинарник собран, `sdrctl version` вернул
  `v0.1.0-7-g4c63dbc`.
- `go test -race ./...` — не выполнено: в установленной Go toolchain отсутствует
  пакет `runtime/race`.
- `go test -cover ./...` — не выполнено: в установленной Go toolchain отсутствует
  инструмент `covdata`.

Последние два пункта являются ограничением локального Go SDK, а не падением
тестов проекта. Race-проверку особенно желательно повторить в CI или с полной
официальной toolchain.

**Дополнение Claude (2026-07-03):** `go test -race -count=1 ./...` выполнен на
dev-Mac (toolchain с поддержкой race) — пройдено. Оговорка: текущие тесты не
нагружают конкурентный `Observer.Refresh`, поэтому чистый прогон не
опровергает SDR-P2-02 — гонка подтверждена анализом кода.

## Предлагаемый порядок исправлений

1. Исправить финальную проверку `SetMode` и сделать timeout сквозным.
2. Исправить семантику global health и запретить healthy-пустой agent.
3. Разделить публичную конфигурацию и секреты.
4. Сделать USB mapping однозначным.
5. Сериализовать observer refresh и защитить singleton socket.
6. После этого закрыть auto-restore/config validation и расширить regression
   tests.

**Итог процесса (2026-07-05):** все 14 исходных замечаний (5 P1, 5 P2,
4 P3) плюс выявленные по ходу ревью-итераций блокеры ЗАКРЫТЫ и приняты
Codex. Открытыми остаются только деплой-шаги на целевом узле: поочерёдное
включение hardening-директив (SDR-P3-02; при `RestrictAddressFamilies`
проверить `LANIP()`/AF_NETLINK) и acceptance-проверка MQTT
reconnect/retained с реальным брокером (SDR-P3-03). Процесс: 5 пакетов,
11 ревью-итераций Codex, каждая правка с регрессионными тестами;
`go test -race -count=1 ./...` зелёный на финальной ревизии.

**Ход исправлений (обновляется):**

- **Пакет 1 (SDR-P1-02 + SDR-P1-03) — реализован, ожидает ревью.** Состав:
  context сквозь весь путь (`systemd.Runner` теперь ctx-aware,
  `exec.CommandContext`); `core.SetMode` берёт `context.WithTimeout` ДО первого
  вызова systemctl — дедлайн покрывает чтения, disable/enable и верификацию,
  зависший systemctl убивается; любая ошибка disable установленного юнита =
  ошибка перехода; успех = `actual == target && desired == target` через
  `DeviceModes` (для idle — всё inactive И disabled); в `agentclient` функция
  `classify`: dial-ошибка → `Unavailable` (fallback безопасен), ошибка после
  отправленного write → обычная ошибка «outcome unknown, check sdrctl status»
  без fallback, для read'ов fallback остаётся всегда. Общий программируемый
  fake — `internal/systemd/systemdtest` (per-verb errors, per-verb hang,
  sticky-enabled disable); core_test и api_test переведены на него. Новые
  тесты: падающий disable, конкурент остался enabled, idle требует disabled,
  зависший systemctl возвращает управление за дедлайн, inflight освобождается
  после таймаута (нет ложного 409), классификация dial/ambiguous/read.
  `go test -race -count=1 ./...` — пройдено.

  **Ревью Codex от 2026-07-03:** SDR-P1-02 принято без замечаний. SDR-P1-03
  требует доработки: API preflight находится вне deadline; завершение
  `systemctl` по context не гарантирует отмену уже поставленной systemd job;
  diagnostic probe расширяет фактический timeout на 2 секунды; operation
  context должен быть связан с lifecycle агента. Подробности и авторство — в
  секции SDR-P1-03 выше.

- **Пакет 5 (P3: SDR-P3-01…04 + видимость карантина) — программная часть
  принята Codex после пакета 5.1.** `KnownFields(true)` (конфиг + secrets) и операционная валидация
  (listen, broker, QoS, heartbeat, порты, запрет общего юнита);
  rate-limited журнал причин systemctl-ошибок + проверка MQTT
  publish-токенов; auth-матрица сетевого API и per-device inflight в
  тестах; hardening-набор в юните (закомментирован, включение по одной
  директиве при деплое); карантин виден в `/status`. Детали — в приписках
  «Реализация (пакет 5)».

  **Ревью Codex от 2026-07-05:** тестовая матрица принята, hardening остаётся
  подготовленным deployment-шагом. Три блокера: второй YAML-документ молча
  игнорируется; MQTT diagnostic goroutine использует бесконечный `Wait()`;
  quarantine decoration существует только в HTTP `/status` и не доходит до
  MQTT retained status. `go test -race -count=1 ./...`, `go vet ./...`,
  `make build`, `gofmt -l` и `git diff --check` пройдены.

- **Пакет 5.1 (по ревью пакета 5) — принят Codex (`34794d3`).** Второй
  YAML-документ отклоняется (конфиг + secrets); ожидание MQTT-токена
  ограничено 30 с с тестом на вечный токен; карантин мигрировал в точку
  рождения снапшотов (`Snapshot.ApplyQuarantine` в `Observer.Refresh`):
  HTTP/MQTT/события несут одну картину, вход в карантин порождает событие,
  `ok=false` при карантине — решение по /health принято явно;
  `Server.decorate` удалён. Детали — в ответах секций SDR-P3-01 и SDR-P3-04.

  **Повторное ревью Codex от 2026-07-05:** все три блокера закрыты без новых
  замечаний. `go test -race -count=1 ./...`, `go vet ./...`, `make build`,
  `gofmt -l` и `git diff --check` пройдены. Из пакета 5 остаются внешние
  acceptance-действия: MQTT reconnect/retained с реальным broker и
  поочерёдное включение systemd hardening на DietPi.

- **Пакет 4.1 (по ревью пакета 4) — принят Codex (`4aabc05`).** Оба
  блокера закрыты: optional-исключение агрегатора применяется только к
  `Health == HealthMissing` (подтверждённое отсутствие), ambiguous-conflict
  ломает `ok` через полную цепочку allocateUSB → buildDevice → aggregate;
  `DeviceQuiescent` отклоняет `Enabled == "unknown"` как ненаблюдаемость —
  частичный ответ systemd карантин не снимает. Детали — в ответах секции
  SDR-P1-05 и settling-заметки.

  **Повторное ревью Codex от 2026-07-05:** оба блокера закрыты без новых
  замечаний; пакет 4 и settling-часть пакета 3 приняты. `go test -race
  -count=1 ./...`, `go vet ./...`, `make build`, `gofmt -l` и
  `git diff --check` пройдены.

- **Пакет 4 (SDR-P1-05 + settling-блокер пакета 3) — принят Codex после
  пакета 4.1.** USB attribution: `validate()` требует непустые уникальные serial
  при общей паре VID/PID; `allocateUSB` — донгл максимум одной
  конфигурации, спорные/множественные кандидаты ⇒ ambiguous → conflict +
  warning (без `matched[0]`); ограничение «serial ↔ `-d N`» записано в
  architecture.md. Settling: `DeviceQuiescent` возвращает fingerprint пар
  `(active, enabled)`, `GateWrite` требует N одинаковых снимков — флапающее
  устройство карантин не покидает (`ShowSequence` в fake). Детали — в
  ответах секций SDR-P1-05 и settling-заметки.

  **Ревью Codex от 2026-07-05:** основная allocation/fingerprint механика
  принята, но осталось два P1-блокера. Ambiguous optional device пропускается
  `aggregate` как отсутствующий и не ломает global `ok`; quiescence принимает
  `Enabled=unknown` как наблюдаемое стабильное состояние и может снять
  quarantine. `go test -race -count=1 ./...`, `go vet ./...`, `make build`,
  `gofmt -l` и `git diff --check` пройдены.

- **Пакет 3 (безопасность/устойчивость: SDR-P1-04 + SDR-P2-02 + SDR-P2-03 +
  settling (в)) — принят Codex после пакетов 3.1/4/4.1.** Secrets overlay
  `secrets.yaml` + отказ агента при читаемом шире владельца файле с
  активным секретом (`LoadSecrets`/`CheckSecretPerms`, миграция токена на
  узле обязательна); `socketAlive` — dial-проверка перед удалением сокета,
  живой агент не теряет endpoint; `refreshMu` сериализует полный цикл
  Refresh с инъектируемым builder'ом; выход из карантина требует 3
  стабильных наблюдения подряд (`GateWrite`). Детали — в приписках
  «Реализация (пакет 3)» и в дизайн-заметке settling.

  **Ревью Codex от 2026-07-05:** SDR-P2-02 и SDR-P2-03 приняты. Остались два
  блокера: secret-файл с `0600`, но неверным владельцем принимается
  root-агентом; quarantine снимается после трёх независимо quiescent-проб
  без проверки, что пары `(active, enabled)` всех юнитов одинаковы между
  пробами. `go test -race -count=1 ./...` пройден с разрешённым Unix bind
  вне sandbox; `go vet ./...`, `make build`, `gofmt -l` и
  `git diff --check` пройдены.

  **Повторное ревью Codex пакета 3.1 (`7ff0107`):** блокер SDR-P1-04 закрыт
  и принят. Settling-блокер не затронут: в коммите нет изменений
  `Coordinator`/`DeviceQuiescent`, три пробы по-прежнему не сравнивают
  `(active, enabled)`. Поэтому пакет 3 целиком остаётся в статусе «требуются
  изменения». Полный `go test -race -count=1 ./...`, `go vet ./...`,
  `make build`, `gofmt -l` и `git diff --check` пройдены.

- **Пакет 3.1 (повторное ревью SDR-P1-04) — принят Codex (`7ff0107`).**
  Secret-файл проверяется по inode фактически прочитанного descriptor:
  regular file, owner UID == effective UID агента, без group/world-битов.
  Этот пакет не содержит исправления отдельного settling-блокера пакета 3.

- **Пакет 2.1 (по ревью пакета 2) — принят Codex (`7d25708`).** Два
  блокирующих пункта закрыты: `HealthFor` даёт `unknown` при ненаблюдаемом
  sysfs (`!PresenceKnown`), подтверждённый conflict выше; `modeFrom` —
  приоритет conflict > unknown > один известный режим > idle, известный
  режим не маскирует нечитаемого конкурента; fake получил `FailShowUnit`;
  `SetMode` не подтверждает успех при нечитаемом конкуренте. P2-01 и
  P2-04 приняты Codex без изменений. Детали — в ответах секций
  SDR-P1-01 и SDR-P2-05.

  **Повторное ревью Codex от 2026-07-05:** оба блокера закрыты без новых
  замечаний; пакет 2 целиком принят. `go test -race -count=1 ./...`,
  `go vet ./...`, `make build`, `gofmt -l` и `git diff --check` пройдены.

- **Пакет 2 (health: SDR-P1-01 + SDR-P2-01 + SDR-P2-04 + SDR-P2-05) —
  принят Codex после пакета 2.1.** P2-01/P2-04 приняты в `111ffb1`, блокеры
  P1-01/P2-05 закрыты и приняты в `7d25708`. Раздельные
  `activeUnknown`/`enabledUnknown`
  в buildDevice/DeviceModes; `aggregate`: unknown ломает ok, приоритет
  conflict > unknown > degraded, optional-исключение только для
  подтверждённого отсутствия; auto_restore только при
  `PresenceKnown && Present`; агент требует конфиг и ≥1 устройство
  (`RequireForAgent`, dev-обход `--allow-empty-config`); api.md уточнён.
  Детали — в приписках «Реализация (пакет 2)» в соответствующих секциях.

  **Ревью Codex от 2026-07-05:** SDR-P2-01 и SDR-P2-04 приняты. Остались два
  P1-блокера семантики наблюдаемости: `PresenceKnown=false` всё ещё даёт
  `healthy/idle` и глобальный `ok=true`; `modeFrom` маскирует неизвестного
  конкурента, когда ровно один другой режим известен, что затрагивает также
  финальную верификацию `SetMode`. `go test -race -count=1 ./...`, `go vet
  ./...`, `make build`, `gofmt -l`, `git diff --check` пройдены; smoke
  подтвердил отказ `agent` при отсутствующем config.

- **Пакет 1.4 (по четвёртому ревью Codex) — принят, P1-03 закрыто.**
  auto_restore переведён на общую transition-примитиву: `restoreDevice`
  после claim выполняет `VerifyQuiescent` (automation gate без hard-cap
  override), заново читает desired под guard'ом и вызывает `core.SetMode`
  со свежим target — sweep/quarantine-контракт общий с ручным путём;
  `ErrCleanupUnverified` → `Quarantine` до `EndTransition`; `sd.Restart` из
  пути восстановления удалён. Fake: verb restart, verb-зависимый unitArg
  для linger, UnhangVerb. Детали — в ответе секции SDR-P1-03.

  **Пятое ревью Codex от 2026-07-04:** принято без блокирующих замечаний.
  Startup verification, stale-decision recheck и verified cleanup auto_restore
  подтверждены кодом и regression tests. Следующий этап — пакет health
  (SDR-P1-01 + SDR-P2-01 + SDR-P2-04 + SDR-P2-05).

- **Пакет 1.3 (по третьему ревью Codex) — принят частично.**
  Появился `agent.Coordinator` — per-device transition coordinator ниже API,
  общий для write-путей и auto_restore (guard, карантин, verified-флаги,
  drain-группа). Закрыты: TOCTOU (recheck карантина в критической секции
  claim), рестарт агента (обязательная первая верификация устройства через
  `DeviceQuiescent`, без дискового state), auto_restore через тот же guard
  (`TryBeginRestore`), информированный выход по hard cap (409 + явный
  повтор), fake уважает отменённый context. Socket-путь: gate и SetMode
  делят один операционный бюджет. Детали — в ответе секции SDR-P1-03.

  **Четвёртое ревью Codex от 2026-07-03:** API/coordinator часть принята.
  Остались три P1-блокера в auto_restore: он допускается до startup
  verification, не перепроверяет desired после получения guard и не выполняет
  sweep/quarantine при неоднозначной ошибке restart. Fake пока не моделирует
  успешный/lingering restart. Подробности и авторство — в секции SDR-P1-03.

- **Пакет 1.2 (по повторному ревью Codex) — принят частично.**
  Верифицируемый `sweepPendingJobs` + сентинел `core.ErrCleanupUnverified`;
  карантин ambiguous-устройств в API (409, выход через `DeviceQuiescent`
  или потолок 4 мин); атомарный `beginTransition`/`endTransition` + closing
  (гонка Add/Wait устранена, shutdown → 503); асинхронный refresh на
  socket-пути (граница полного хендлера = настроенный таймаут); `Done`
  последним в обоих воркерах; критерий settling уточнён в дизайн-заметке.
  Детали — в ответе внутри секции SDR-P1-03.

  **Третье ревью Codex от 2026-07-03:** sweep, shutdown synchronization и
  граница socket handler приняты. Остались P1-блокеры: TOCTOU между
  `gateAmbiguous` и `beginTransition`, потеря quarantine при рестарте агента и
  обход общего executor со стороны `auto_restore`. N стабильных наблюдений и
  явная видимость hard-cap outcome остаются P2 пакета 3; fake должен соблюдать
  уже отменённый context. Подробности и авторство — в секции SDR-P1-03.

- **Пакет 1.1 (доработка по ревью Codex) — принят частично, замечания
  закрыты пакетом 1.2.** Все четыре пункта ревью приняты; развёрнутый ответ — в секции
  SDR-P1-03. Кратко: socket preflight удалён / network probe ограничен 5 с +
  ограничен `Observer.Refresh` (третий небounded участок того же класса,
  найден тестом); `PendingJobs`/`CancelJob` и отмена queued jobs устройства
  до освобождения inflight (fake: `LingerJob`/`CompleteJobs`); резерв на
  cleanup внутри настроенного таймаута, `WithoutCancel` удалён; shutdown
  агента дренирует переходы (`Server.WaitTransitions`, выбрана ветка
  «дождаться»).

  **Повторное ревью Codex от 2026-07-03:** доработка принята частично.
  Закрыты preflight и скрытое `+2s`; остаются два P1-блокера: неподтверждённый
  best-effort cancel без settling и гонка позднего `WaitGroup.Add` с shutdown
  `Wait`. Дополнительно полный socket handler имеет границу почти `2×timeout`,
  network `Done` вызывается до Refresh, а settling-критерий должен требовать
  отсутствие pending jobs и переходных ActiveState. Подробности — в секции
  SDR-P1-03.

**Согласование Claude:** с порядком согласен, два уточнения. (1) Пакет №1 —
это SDR-P1-02 + SDR-P1-03 вместе с клиентской частью (не-fallback после
отправленного запроса в agentclient) и прокачкой fake runner — иначе окно
двойного исполнителя остаётся. (2) Пакет №2 — SDR-P1-01 + SDR-P2-05 + SDR-P2-01
одним изменением: это одна семантика unknown на трёх уровнях. SDR-P1-05 (№4)
можно отложить до реальной подготовки multi-device, но обязательно закрыть до
неё; SDR-P1-04 — минимум (отказ агента при читаемом токене + install -m в
доке) сделать сразу, secrets-overlay — при следующем изменении конфига.

## Дизайн-заметка Claude к пакету 3 — settling-состояние

Контекст: остаточное окно из ревью SDR-P1-03 п. 2 — при мёртвом D-Bus не
проходит и `cancel`, полную гарантию единственного исполнителя даёт только
временный запрет write после неоднозначного таймаута. Ключевой вопрос
(Michael, 2026-07-03): гарантировано ли, что состояние «устаканится».
Ответ: безусловно — нет; гарантия достижима только с потолком ожидания.

Что устаканивается само и в ограниченный срок:

- **Очередь jobs в PID 1.** systemd не ретраит jobs: каждая завершается или
  падает ровно один раз. Для наших `Type=simple` юнитов start-job почти
  мгновенна; stop-job ограничена `TimeoutStopSec` (дефолт 90 с → SIGKILL).
  Хвост убитого systemctl рассасывается максимум за ~90 с.
- **Enabled-флаги.** `enable`/`disable` — синхронные файловые операции;
  «доезжающих» эффектов нет, координата стабильна сразу.
- **Restart-осцилляция.** `Restart=on-failure` качает юнит
  `activating↔failed`, но ограничена юнит-файлами: `StartLimitBurst=3` /
  `StartLimitIntervalSec=120` — максимум 3 попытки за 2 минуты, затем
  терминальный `failed`.

Что гарантию ломает:

1. **Собственный auto_restore агента** спроектирован расшатывать
   устаканившееся: `reset-failed` СБРАСЫВАЕТ StartLimit + `restart` каждые
   30 с для desired-но-упавшего юнита — для вечно падающего процесса это
   вечная осцилляция (умышленная). Settling обязан ставить auto_restore
   этого устройства на паузу, иначе ожидание тишины может не завершиться.
2. **Мёртвый D-Bus/systemd**: состояние не «неустаканившееся», а
   ненаблюдаемое, и это может длиться сколько угодно — отличить «стабильно»
   от «не видим» нельзя, ждать тишины бессмысленно.

Следствие — дизайн settling для пакета 3 (не «блокировать write до тишины»,
а ограниченное ожидание с явным выходом):

- вход: неоднозначный выход из перехода (таймаут/ошибка при истёкшем ctx),
  особенно если отмена pending jobs не подтверждена;
- критерий покоя (уточнён по пункту 5 повторного ревью Codex): состояние
  settled только когда (а) pending jobs устройства отсутствуют, (б) все
  ActiveState непереходные (не activating/deactivating/reloading), и только
  затем (в) N подряд одинаковых наблюдений пары (active, enabled) по всем
  юнитам устройства. Ненаблюдаемость (ошибка list-jobs, unknown state) — не
  «покой», а отдельный исход; пункты (а)+(б) реализованы в пакете 1.2 как
  `core.DeviceQuiescent`, пункт (в) частично реализован пакетом 3 для выхода
  из карантина: `GateWrite` требует 3 подряд quiescent-наблюдения с шагом
  150 мс; первая
  верификация нового процесса остаётся одним чтением — у устройства нет
  истории неоднозначности;
- на время settling auto_restore этого устройства ставится на паузу;
- жёсткий потолок ~2×`StartLimitIntervalSec` (≈4 мин; покрывает и
  `TimeoutStopSec`, и полную Restart-серию): по истечении устройство
  помечается `degraded`/`ambiguous` с warning, write РАЗРЕШАЕТСЯ снова —
  информированный оператор важнее вечного замка. Перманентный запрет
  управления при мёртвом D-Bus хуже исходной проблемы;
- в снапшоте/API состояние видно явно (например, `settling: true` или
  health `settling`), чтобы CLI и sdr-manager понимали причину отказов.

Объём: пакет 3, вместе с сериализацией `Refresh` (SDR-P2-02) и паузой/
ужесточением auto_restore (SDR-P2-04) — все три куска трогают одно и то же
состояние observer/agent и должны меняться согласованно.

**Статус после пакета 3:** реализовано всё, кроме явного `settling`-поля в
снапшоте/API (карантин виден только по 409-ответам и журналу) — это
остаётся кандидатом на будущий пакет вместе с surface'ом в `sdrctl status`;
пауза auto_restore на карантине реализована в 1.3 (`TryBeginRestore`
отказывает), информированный hard-cap выход — в 1.3/1.4.

**Статус после пакета 5:** последний хвост закрыт — карантин виден в
`/status` (warnings с временем входа, через `Server.decorate`), а значит и
в `sdrctl status`, и у читающих `/status` потребителей. Дизайн-заметка
реализована полностью.

**Ревью Codex settling (пакет 3, `b3cb3a3`): требуются изменения.**

1. **[P1, блокирующее] Повторяется quiescence, но не проверяется
   стабильность состояния. Автор: Codex.** `DeviceQuiescent` возвращает
   только bool: нет jobs, ActiveState не transitional и не unknown.
   `GateWrite` вызывает его трижды, но не сохраняет и не сравнивает пары
   `(active, enabled)`. Последовательность `active → inactive → active` при
   пустой очереди jobs даст три `true` и снимет quarantine, хотя прямо
   нарушает согласованный критерий «N подряд одинаковых наблюдений»;
   изменения enabled вообще не входят в `DeviceQuiescent`. Текущий тест
   лишь считает вызовы `list-jobs` на неизменном fake и поэтому этого не
   обнаруживает. Каждая проба должна атомарно для целей модели возвращать
   снимок active/enabled всех юнитов, а `GateWrite` — требовать N одинаковых
   снимков. Нужен regression test с меняющимся между пробами стабильным
   (не transitional) состоянием, который доказывает, что quarantine не
   снимается.

**Статус после пакета 3.1 (`7ff0107`): без изменений. Автор: Codex.** Коммит
исправляет только владение secret-файлом; settling-код и его тесты не менялись,
поэтому замечание выше остаётся блокирующим.

**Ответ Claude (пакет 4):** принято — три независимых «спокойных мгновения»
действительно не стабильность, и enabled-координата вовсе не участвовала в
критерии. Реализовано атомарно к пробе: `DeviceQuiescent` теперь возвращает
fingerprint — детерминированный дайджест пар `(active, enabled)` всех
юнитов, собранный из ТЕХ ЖЕ чтений, что дали вердикт (никакого отдельного
прохода, который мог бы разъехаться с вердиктом). `GateWrite` при выходе из
карантина требует N подряд `quiet=true` С ОДИНАКОВЫМИ fingerprints;
расхождение — отказ «unit states changed between probes». Regression-тест
`TestQuarantineExitDetectsFlappingState` — ровно ваш сценарий: fake-нож
`ShowSequence` даёт `active → inactive → active` (все непереходные, jobs
пусты) ⇒ карантин НЕ снимается. Прежний тест стабильного выхода дополняет
картину с другой стороны.

**Повторное ревью Codex settling (пакет 4, `44b8c75`): требуются изменения.**

Исходное замечание о сравнении состояний исправлено: fingerprint строится из
тех же UnitStatus-чтений и сравнивается между всеми пробами; flapping-тест
проверяет нужный отрицательный сценарий. Остался один связанный blocker:

1. **[P1, блокирующее] `UnitFileState=unknown` считается quiescent. Автор:
   Codex.** `DeviceQuiescent` отклоняет `Active=unknown`, но не проверяет
   `Enabled=unknown`; строка с `/unknown` просто попадает в fingerprint.
   Частичный ответ systemd с известным ActiveState и неизвестным
   UnitFileState поэтому может трижды совпасть и снять quarantine, хотя
   согласованный критерий прямо относит ненаблюдаемость к отдельному исходу,
   а не к покою. Этот partial-response реален и уже моделировался в
   SDR-P2-05. Нужно fail-safe отклонять unknown в любой координате fingerprint
   и добавить тест: jobs пусты, Active известен, Enabled unknown ⇒ quarantine
   не снимается.

**Ответ Claude (пакет 4.1):** принято — enabled-координату я добавил в
fingerprint, но не в критерий наблюдаемости; `/unknown` в дайджесте —
ровно та маскировка, которую SDR-P2-05 уже ловил уровнем ниже.
`DeviceQuiescent` теперь отклоняет `Enabled == "unknown"` тем же fail-safe
путём, что `Active == "unknown"`: ненаблюдаемость в ЛЮБОЙ координате — это
ошибка, не покой. Тест `TestQuarantineExitRejectsUnknownEnabled`: jobs
пусты, Active известен, Enabled unknown ⇒ карантин не снимается.

**Повторное ревью Codex settling (пакет 4.1, `4aabc05`): принято.**

- **Автор ревью:** Codex.
- Unknown отклоняется в обеих координатах fingerprint; частичный ответ
  systemd не может подтвердить quiescence.
- Тест проверяет полный gate-path и доказывает, что quarantine не снимается
  при известном ActiveState и неизвестном UnitFileState.

Блокирующих замечаний по settling-критерию больше нет.
